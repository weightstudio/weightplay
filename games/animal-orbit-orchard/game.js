(function () {
  "use strict";
  const locales = window.ORBIT_ORCHARD_LOCALES;
  const rounds = [{ angle: 42, name: "Dew Beacon" }, { angle: 188, name: "Moon Apple" }, { angle: 306, name: "Star Pear" }];
  const routeMap = { en: "en", "zh-tw": "zh-Hant", "zh-hant": "zh-Hant", "zh-cn": "zh-Hans", "zh-hans": "zh-Hans" };
  const state = { locale: "en", sound: true, round: 0, angle: 0, releases: 0, selected: 0 };
  const $ = (id) => document.getElementById(id);
  const storage = { get(key) { try { return localStorage.getItem(key); } catch (_) { return null; } }, set(key, value) { try { localStorage.setItem(key, value); } catch (_) {} } };
  const t = (key, vars = {}) => { const copy = locales[state.locale] || locales.en; return String(copy[key] || locales.en[key] || key).replace(/\{(\w+)\}/g, (_, name) => String(vars[name] ?? "")); };
  function queryLocale() { const q = new URLSearchParams(location.search).get("lang"); if (q && locales[q]) return q; const segment = location.pathname.split("/").filter(Boolean)[0]?.toLowerCase(); return routeMap[segment] || storage.get("weightplay-orbit-locale") || "en"; }
  function tone(kind) { if (!state.sound || !(window.AudioContext || window.webkitAudioContext)) return; try { const C = window.AudioContext || window.webkitAudioContext; const ctx = new C(); const osc = ctx.createOscillator(); const gain = ctx.createGain(); osc.frequency.value = kind === "good" ? 640 : 190; gain.gain.setValueAtTime(.0001, ctx.currentTime); gain.gain.exponentialRampToValueAtTime(.025, ctx.currentTime + .01); gain.gain.exponentialRampToValueAtTime(.0001, ctx.currentTime + .1); osc.connect(gain).connect(ctx.destination); osc.start(); osc.stop(ctx.currentTime + .11); osc.addEventListener("ended", () => ctx.close(), { once: true }); } catch (_) {} }
  function applyLocale() { const copy = locales[state.locale] || locales.en; document.documentElement.lang = state.locale === "zh-Hant" ? "zh-TW" : state.locale; document.documentElement.dir = copy.direction || "ltr"; document.querySelectorAll("[data-copy]").forEach((node) => { node.textContent = t(node.dataset.copy); }); $("settingsBtn").setAttribute("aria-label", t("settings")); $("settingsPanel").setAttribute("aria-label", t("settings")); $("soundBtn").textContent = t(state.sound ? "soundOn" : "soundOff"); $("angleInput").setAttribute("aria-label", t("dial")); renderStage(); if (!$('battleScreen').hidden) renderBattle(); }
  function populateLocales() { const select = $("localeSelect"); locales.__localeKeys.forEach((key) => { const option = document.createElement("option"); option.value = key; option.textContent = locales.en.languageNames[key] || key; select.append(option); }); select.value = state.locale; select.addEventListener("change", () => { state.locale = select.value; storage.set("weightplay-orbit-locale", state.locale); applyLocale(); }); }
  function show(screen) { ["mainScreen", "stageScreen", "battleScreen", "resultScreen"].forEach((id) => { $(id).hidden = id !== screen; }); $("mainScreen").parentElement.querySelector(".guide-card")?.toggleAttribute("hidden", screen !== "mainScreen"); document.body.dataset.screen = screen.replace("Screen", ""); window.scrollTo(0, 0); }
  function renderStage() { if (!$('stageScreen') || $('stageScreen').hidden) return; const list = $("orbitList"); list.replaceChildren(...rounds.map((item, index) => { const button = document.createElement("button"); button.type = "button"; button.className = "orbit-card"; button.innerHTML = `<strong>${t("orbit", { current: index + 1, total: rounds.length })}</strong><span>${item.name}</span><em>${t("target", { angle: item.angle })}</em>`; button.addEventListener("click", () => { state.round = index; startRound(); }); return button; })); }
  function start() { state.round = 0; state.releases = 0; openMap(); }
  function startRound() { state.angle = 0; show("battleScreen"); renderBattle(); $("angleInput").focus(); }
  function renderBattle() { const item = rounds[state.round]; $("roundLabel").textContent = t("orbit", { current: state.round + 1, total: rounds.length }); $("targetLabel").textContent = t("target", { angle: item.angle }); $("releaseCount").textContent = String(state.releases); $("angleInput").value = String(state.angle); $("angleReadout").textContent = `${state.angle}°`; $("beacon").style.transform = `rotate(${item.angle}deg) translateX(105px)`; $("seed").style.transform = `rotate(${state.angle}deg) translateX(78px)`; if (!$('battleStatus').textContent) $('battleStatus').textContent = t("ready"); }
  function shortestDelta(a, b) { const d = Math.abs(a - b) % 360; return Math.min(d, 360 - d); }
  function release() { state.releases += 1; $("releaseCount").textContent = String(state.releases); const target = rounds[state.round].angle; const delta = shortestDelta(state.angle, target); if (delta <= 16) { $("battleStatus").textContent = t("close"); $("battleStatus").className = "battle-status is-good"; tone("good"); window.setTimeout(() => { if (state.round < rounds.length - 1) { state.round += 1; state.angle = 0; $("battleStatus").textContent = t("ready"); $("battleStatus").className = "battle-status"; renderBattle(); $("angleInput").focus(); } else finish(); }, 360); return; } const side = ((state.angle - target + 360) % 360) < 180 ? "ahead" : "behind"; $("battleStatus").textContent = `${t("miss", { delta })} ${t(side)}`; $("battleStatus").className = "battle-status is-miss"; tone("miss"); }
  function finish() { const key = "weightplay-orbit-orchard-best-releases"; const prior = Number(storage.get(key)); if (!prior || state.releases < prior) storage.set(key, String(state.releases)); $("resultText").textContent = t("finishText", { releases: state.releases }); $("bestValue").textContent = storage.get(key) || String(state.releases); show("resultScreen"); }
  function goHome() { show("mainScreen"); applyLocale(); }
  function openMap() { show("stageScreen"); renderStage(); }
  function toggleSettings() { const panel = $("settingsPanel"); const open = panel.hidden; panel.hidden = !open; $("settingsBtn").setAttribute("aria-expanded", String(open)); }
  state.locale = queryLocale();
  document.addEventListener("DOMContentLoaded", () => { populateLocales(); applyLocale(); window.setTimeout(() => { $("loadingPanel").hidden = true; $("app").hidden = false; }, 260); $("startBtn").addEventListener("click", start); $("replayBtn").addEventListener("click", start); $("homeBtn").addEventListener("click", goHome); $("stageBackBtn").addEventListener("click", goHome); $("battleBackBtn").addEventListener("click", openMap); $("mapBtn").addEventListener("click", openMap); $("resultMapBtn").addEventListener("click", openMap); $("releaseBtn").addEventListener("click", release); $("clearBtn").addEventListener("click", () => { state.angle = 0; $("battleStatus").textContent = t("ready"); $("battleStatus").className = "battle-status"; renderBattle(); }); $("angleInput").addEventListener("input", (event) => { state.angle = Number(event.target.value); renderBattle(); }); $("settingsBtn").addEventListener("click", toggleSettings); $("soundBtn").addEventListener("click", () => { state.sound = !state.sound; applyLocale(); }); });
  window.ORBIT_ORCHARD_TEST = { rounds, start, release, renderBattle };
})();
