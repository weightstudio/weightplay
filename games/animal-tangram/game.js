(()=>{
  "use strict";
  const codes=["en","zh-Hant","zh-Hans","ja","ko","es","pt-BR","fr","de","it","ru","hi","ar"],levels=window.TANGRAM_LEVELS.levels,parts=window.TANGRAM_LEVELS.pieces;
  const $=s=>document.querySelector(s),screens=[...document.querySelectorAll(".screen")],key="wp-animal-tangram-v1";
  let locale=read("wp-locale")||"en";if(!codes.includes(locale))locale="en";let unlocked=Number(read(key))||1,selected=Math.min(unlocked,30)-1,level=null,states=[],moves=0,drag=null;
  function read(k){try{return localStorage.getItem(k)}catch{return null}}function write(k,v){try{localStorage.setItem(k,v)}catch{}}
  function t(k,v={}){const value=window.TANGRAM_LOCALES[locale]?.[k]??window.TANGRAM_LOCALES.en[k]??k;return String(value).replace(/\{(\w+)\}/g,(_,n)=>v[n]??"")}
  function show(id){screens.forEach(screen=>screen.hidden=screen.id!==id);document.body.dataset.screen=id;if(id==="stage")renderStages()}
  function renderStages(){$("#progress").textContent=t("progress",{done:Math.min(unlocked-1,30)});$("#stageGrid").innerHTML="";levels.forEach((item,index)=>{const b=document.createElement("button"),locked=index+1>unlocked;b.className="stage-card"+(index===selected?" selected":"");b.disabled=locked;b.innerHTML="<strong>"+t("shape",{n:index+1})+"</strong><span>"+item.names[0]+(locked?" · "+t("locked"):"")+"</span>";b.onclick=()=>{selected=index;renderStages()};$("#stageGrid").append(b)});requestAnimationFrame(()=>$("#stageGrid .selected")?.scrollIntoView({block:"nearest"}))}
  function pos(el,x,y,r){el.style.left=(x-.085)*100+"%";el.style.top=(y-.085)*100+"%";el.style.transform="rotate("+r+"deg)"}
  function startLevel(index){selected=index;level=levels[index];states=parts.map(()=>({x:.18,y:.86,r:0,placed:false}));states.forEach((s,i)=>{s.x=.18+(i%4)*.22;s.y=.86+Math.floor(i/4)*.1});moves=0;$("#chapter").textContent=t("chapter",{n:Math.floor(index/5)+1});$("#stageName").textContent=t("shape",{n:index+1});$("#status").textContent=t("status");show("battle");renderBoard()}
  function renderBoard(){const targets=$("#targets"),piecesLayer=$("#pieces");targets.innerHTML="";piecesLayer.innerHTML="";level.targets.forEach((target,index)=>{const ghost=document.createElement("div");ghost.className="target-piece "+parts[index];ghost.dataset.shape=parts[index];pos(ghost,target.x,target.y,target.r);targets.append(ghost)});states.forEach((state,index)=>{const piece=document.createElement("button");piece.className="piece "+parts[index]+(state.placed?" placed":"");piece.dataset.shape=parts[index];piece.dataset.index=index;piece.setAttribute("aria-label",t("pieceLabel",{n:index+1}));pos(piece,state.x,state.y,state.r);piece.onpointerdown=event=>begin(index,event);piece.onpointermove=event=>move(event);piece.onpointerup=finish;piece.onpointercancel=finish;piecesLayer.append(piece)});$("#placedCount").textContent=t("placed",{done:states.filter(state=>state.placed).length})}
  function begin(index,event){const state=states[index];state.placed=false;drag={index,pointer:event.pointerId,x:event.clientX,y:event.clientY,startX:state.x,startY:state.y,moved:false};event.currentTarget.setPointerCapture?.(event.pointerId);event.currentTarget.classList.remove("placed")}
  function move(event){if(!drag)return;const rect=$("#board").getBoundingClientRect(),state=states[drag.index];state.x=Math.max(.07,Math.min(.93,drag.startX+(event.clientX-drag.x)/rect.width));state.y=Math.max(.07,Math.min(.93,drag.startY+(event.clientY-drag.y)/rect.height));drag.moved=true;const piece=document.querySelector("[data-index="+drag.index+"]");if(piece)pos(piece,state.x,state.y,state.r)}
  function finish(){if(!drag)return;const index=drag.index,state=states[index],target=level.targets[index],angle=Math.abs(((state.r-target.r+540)%360)-180),distance=Math.hypot(state.x-target.x,state.y-target.y);if(!drag.moved){state.r=(state.r+45)%360;$("#status").textContent=t("status");renderBoard()}else if(distance<.075&&angle<=1){state.x=target.x;state.y=target.y;state.r=target.r;state.placed=true;moves++;$("#status").textContent=t("snap");renderBoard();checkComplete()}else{$("#status").textContent=t("wrong");renderBoard()}drag=null}
  function checkComplete(){if(states.every(state=>state.placed)){if(selected+2>unlocked){unlocked=Math.min(31,selected+2);write(key,String(unlocked))}$("#resultBody").textContent=t("resultBody",{n:selected+1,moves});$("#next").textContent=selected===29?t("allDone"):t("next");setTimeout(()=>$("#result").showModal(),180)}}
  function hint(){const index=states.findIndex(state=>!state.placed);if(index<0)return;const target=level.targets[index];states[index]={x:target.x,y:target.y,r:target.r,placed:true};moves++;$("#status").textContent=t("snap");renderBoard();checkComplete()}
  function applyLocale(){document.documentElement.lang=locale;document.documentElement.dir=locale==="ar"?"rtl":"ltr";document.title=t("title")+" | WeightPlay";document.querySelectorAll("[data-t]").forEach(node=>node.textContent=t(node.dataset.t));document.querySelectorAll("[data-t-aria]").forEach(node=>node.setAttribute("aria-label",t(node.dataset.tAria)));document.querySelectorAll("[data-t-alt]").forEach(node=>node.setAttribute("alt",t(node.dataset.tAlt)));$("#locale").value=locale;if(!$("#stage").hidden)renderStages();if(!$("#battle").hidden)renderBoard()}
  codes.forEach(code=>{const option=document.createElement("option");option.value=code;option.textContent=window.TANGRAM_LOCALES[code].label;$("#locale").append(option)});$("#locale").onchange=e=>{locale=e.target.value;write("wp-locale",locale);applyLocale()};
  $("#start").onclick=()=>show("stage");$("#enter").onclick=()=>startLevel(selected);document.querySelectorAll("[data-back]").forEach(button=>button.onclick=()=>show(button.closest("#battle")?"stage":"main"));$("#hint").onclick=hint;$("#reset").onclick=()=>startLevel(selected);$("#retry").onclick=()=>{$("#result").close();startLevel(selected)};$("#next").onclick=()=>{$("#result").close();selected===29?show("stage"):startLevel(selected+1)};applyLocale();show("main");
})();
