(() => {
  "use strict";
  const $ = (selector) => document.querySelector(selector);
  const SAVE_KEY = "weightplay_animal_2048_v1";
  const TUTORIAL_KEY = "weightplay_tutorial_seen_animal_2048_v1";
  const LOCALE_SEGMENTS = {en:"en","zh-tw":"zh-Hant","zh-cn":"zh-Hans",es:"es",ja:"ja"};
  const SEGMENTS = {en:"en","zh-Hant":"zh-tw","zh-Hans":"zh-cn",es:"es",ja:"ja"};
  const firstSegment = location.pathname.split("/").filter(Boolean)[0] || "en";
  let locale = LOCALE_SEGMENTS[firstSegment] || localStorage.getItem("weightPlayLocale") || "en";

  const EN={title:"Animal 2048",fullTitle:"Animal 2048: Forest Evolution",language:"Language",eyebrow:"Moonlit Forest Puzzle Sanctuary",pitch:"Swipe the whole grid, merge matching animals, and protect enough open space to complete each mission.",start:"Start Game",chooseStage:"Choose Mission",moves:"Moves",score:"Score",undo:"Undo",restart:"Restart",hint:"Hint",retry:"Retry",stages:"Stages",next:"Next Stage",skillReport:"Planning Report",leaveTitle:"Leave this mission?",continue:"Continue",returnStages:"Return to Stages",tutorialTitle:"Slide the whole forest grid.",tutorial1:"Swipe up, down, left, or right.",tutorial2:"Matching animals merge once per move.",tutorial3:"Keep a corner stable and protect empty spaces.",tutorialStart:"Start Mission 1",progress:"{cleared}/30 missions cleared · {stars} stars",mission:"Mission",ready:"Ready",locked:"Locked",cleared:"Cleared",goalTile:"Create {animal} ({value})",goalScore:"Score {target} points",goalMerges:"Complete {target} merges",objectiveTile:"Create {animal} ({value}) · {left} moves left",objectiveScore:"Reach {target} points · {left} moves left",objectiveMerges:"Complete {target} merges · {left} moves left",invalid:"That direction does not move the board.",undoReady:"Previous turn restored.",undoEmpty:"Make a move before using Undo.",hintMove:"Orla suggests: {direction}",noHint:"No improving move is available.",winTitle:"Mission Complete",loseTitle:"Mission Interrupted",winText:"You completed Mission {stage} with {moves} moves and {score} points.",loseMoves:"The move limit ended. Retry is free.",loseBoard:"No legal move remains. Retry is free.",newBest:"New best score for this mission!",best:"Best score: {score}",planning:"Planning",space:"Open Space",evolution:"Evolution",planningValue:"{moves} moves",spaceValue:"{empty} open",evolutionValue:"Tier {tier}",leaveText:"Your current Mission {stage} board will be discarded. Saved clears and stars stay safe.",chapter1:"First Tracks",chapter2:"Chain Grove",chapter3:"Moss Stones",chapter4:"Score Lanterns",chapter5:"Rare Nests",chapter6:"Grand Evolution",rule1:"Slide every tile together and merge equal animals.",rule2:"Prepare two-step and three-step merge chains.",rule3:"Stone cells split rows and columns into separate paths.",rule4:"Build efficient chains to reach the score target.",rule5:"Protect open spaces while evolving rare animals.",rule6:"Combine stones, move limits, and high-tier evolution.",up:"up",down:"down",left:"left",right:"right",mouse:"Mouse",rabbit:"Rabbit",fox:"Fox",penguin:"Penguin",koala:"Koala",owl:"Owl",panda:"Panda",lion:"Lion Cub",giraffe:"Giraffe",elephant:"Elephant",lionKing:"Lion Guardian"};
  const STRINGS={en:EN,"zh-Hant":{...EN,title:"動物 2048",fullTitle:"動物 2048：森林進化",language:"語言",eyebrow:"月光森林解謎聖所",pitch:"滑動整個棋盤、合併相同動物，保留足夠空位並完成每個任務。",start:"開始遊戲",chooseStage:"選擇任務",moves:"步數",score:"分數",undo:"復原",restart:"重新開始",hint:"提示",retry:"再試一次",stages:"關卡",next:"下一關",skillReport:"規劃報告",leaveTitle:"要離開這個任務嗎？",continue:"繼續遊戲",returnStages:"回到關卡",tutorialTitle:"滑動整座森林棋盤。",tutorial1:"向上、下、左或右滑動。",tutorial2:"相同動物相遇後，每回合只會合併一次。",tutorial3:"固定一個角落並保留空格，才能持續進化。",tutorialStart:"開始第 1 關",progress:"已完成 {cleared}/30 關 · {stars} 顆星",mission:"任務",ready:"可挑戰",locked:"未解鎖",cleared:"已完成",goalTile:"進化出{animal}（{value}）",goalScore:"取得 {target} 分",goalMerges:"完成 {target} 次合併",objectiveTile:"進化出{animal}（{value}）· 剩餘 {left} 步",objectiveScore:"取得 {target} 分 · 剩餘 {left} 步",objectiveMerges:"完成 {target} 次合併 · 剩餘 {left} 步",invalid:"這個方向無法移動棋盤。",undoReady:"已復原上一回合。",undoEmpty:"移動後才能使用復原。",hintMove:"奧菈建議：向{direction}",noHint:"目前沒有更好的移動方向。",winTitle:"任務完成",loseTitle:"任務中斷",winText:"你用 {moves} 步與 {score} 分完成第 {stage} 關。",loseMoves:"步數已用完，重新挑戰不需消耗資源。",loseBoard:"棋盤已沒有合法移動，重新挑戰不需消耗資源。",newBest:"這一關的新最佳分數！",best:"最佳分數：{score}",planning:"規劃",space:"空格",evolution:"進化",planningValue:"{moves} 步",spaceValue:"剩 {empty} 格",evolutionValue:"第 {tier} 階",leaveText:"目前第 {stage} 關的棋盤會被捨棄，已保存的完成紀錄與星星不受影響。",chapter1:"起步足跡",chapter2:"連鎖樹林",chapter3:"苔石小徑",chapter4:"分數燈林",chapter5:"稀有獸巢",chapter6:"終極進化",rule1:"滑動全部格子，讓相同動物合併。",rule2:"預先安排兩段與三段合併連鎖。",rule3:"石塊會把直線分成不同移動區段。",rule4:"用有效率的連鎖達成分數目標。",rule5:"保留空格，同時進化稀有動物。",rule6:"綜合石塊、步數與高階進化。",up:"上",down:"下",left:"左",right:"右",mouse:"小鼠",rabbit:"兔子",fox:"狐狸",penguin:"企鵝",koala:"無尾熊",owl:"貓頭鷹",panda:"貓熊",lion:"幼獅",giraffe:"長頸鹿",elephant:"大象",lionKing:"獅王守護者"},"zh-Hans":{...EN,title:"动物 2048",fullTitle:"动物 2048：森林进化",language:"语言",eyebrow:"月光森林解谜圣所",pitch:"滑动整个棋盘、合并相同动物，保留足够空位并完成每个任务。",start:"开始游戏",chooseStage:"选择任务",moves:"步数",score:"分数",undo:"撤销",restart:"重新开始",hint:"提示",retry:"再试一次",stages:"关卡",next:"下一关",skillReport:"规划报告",leaveTitle:"要离开这个任务吗？",continue:"继续游戏",returnStages:"返回关卡",tutorialTitle:"滑动整座森林棋盘。",tutorial1:"向上、下、左或右滑动。",tutorial2:"相同动物相遇后，每回合只会合并一次。",tutorial3:"固定一个角落并保留空格，才能持续进化。",tutorialStart:"开始第 1 关",progress:"已完成 {cleared}/30 关 · {stars} 颗星",mission:"任务",ready:"可挑战",locked:"未解锁",cleared:"已完成",goalTile:"进化出{animal}（{value}）",goalScore:"取得 {target} 分",goalMerges:"完成 {target} 次合并",objectiveTile:"进化出{animal}（{value}）· 剩余 {left} 步",objectiveScore:"取得 {target} 分 · 剩余 {left} 步",objectiveMerges:"完成 {target} 次合并 · 剩余 {left} 步",invalid:"这个方向无法移动棋盘。",undoReady:"已撤销上一回合。",undoEmpty:"移动后才能使用撤销。",hintMove:"奥菈建议：向{direction}",noHint:"目前没有更好的移动方向。",winTitle:"任务完成",loseTitle:"任务中断",winText:"你用 {moves} 步与 {score} 分完成第 {stage} 关。",loseMoves:"步数已用完，重新挑战不需消耗资源。",loseBoard:"棋盘已没有合法移动，重新挑战不需消耗资源。",newBest:"这一关的新最佳分数！",best:"最佳分数：{score}",chapter1:"起步足迹",chapter2:"连锁树林",chapter3:"苔石小径",chapter4:"分数灯林",chapter5:"稀有兽巢",chapter6:"终极进化",up:"上",down:"下",left:"左",right:"右"},es:{...EN,title:"Animal 2048",fullTitle:"Animal 2048: Evolución del Bosque",language:"Idioma",eyebrow:"Santuario de puzles del bosque lunar",pitch:"Desliza toda la cuadrícula, fusiona animales iguales y conserva espacios para completar cada misión.",start:"Comenzar juego",chooseStage:"Elegir misión",moves:"Movimientos",score:"Puntos",undo:"Deshacer",restart:"Reiniciar",hint:"Pista",retry:"Reintentar",stages:"Niveles",next:"Siguiente nivel",skillReport:"Informe de planificación",leaveTitle:"¿Salir de esta misión?",continue:"Continuar",returnStages:"Volver a niveles",tutorialTitle:"Desliza toda la cuadrícula.",tutorial1:"Desliza arriba, abajo, izquierda o derecha.",tutorial2:"Los animales iguales se fusionan una vez por turno.",tutorial3:"Mantén una esquina estable y protege los espacios vacíos.",tutorialStart:"Iniciar misión 1",mission:"Misión",ready:"Lista",locked:"Bloqueada",cleared:"Completada",up:"arriba",down:"abajo",left:"la izquierda",right:"la derecha"},ja:{...EN,title:"アニマル2048",fullTitle:"アニマル2048：森の進化",language:"言語",eyebrow:"月夜の森のパズル聖域",pitch:"盤面全体をスライドし、同じ動物を合体させ、空きマスを守ってミッションを達成しよう。",start:"ゲーム開始",chooseStage:"ミッション選択",moves:"手数",score:"スコア",undo:"元に戻す",restart:"やり直す",hint:"ヒント",retry:"もう一度",stages:"ステージ",next:"次のステージ",skillReport:"計画レポート",leaveTitle:"このミッションを離れますか？",continue:"続ける",returnStages:"ステージへ戻る",tutorialTitle:"森の盤面全体を動かそう。",tutorial1:"上、下、左、右へスワイプします。",tutorial2:"同じ動物は1ターンに1回だけ合体します。",tutorial3:"大きな動物を角に置き、空きマスを守ります。",tutorialStart:"ミッション1開始",mission:"ミッション",ready:"挑戦可能",locked:"ロック",cleared:"クリア",up:"上",down:"下",left:"左",right:"右"}};
  if(!STRINGS[locale])locale="en";
  const t=(key,vars={})=>String(STRINGS[locale][key]??EN[key]??key).replace(/\{(\w+)\}/g,(_,name)=>vars[name]??"");
  const tileNames=["", "mouse","rabbit","fox","penguin","koala","owl","panda","lion","giraffe","elephant","lionKing"];
  const tileAssets=Array.from({length:11},(_,i)=>`../../assets/animal-merge-token-${i>=7&&i<=10&&[7,10].includes(i)?`${i}-boom-lion`:i}.png`);

  const masks=[[],[],[],[],[],[],[],[],[],[],[5],[10],[5,10],[6,9],[1,14],[5],[10],[5,10],[6,9],[1,14],[5,10],[1,14],[4,11],[6,9],[5,10],[1,14],[4,11],[5,10],[6,9],[1,14]];
  const goals=[
    ["tile",8,18],["tile",16,20],["merges",6,22],["tile",32,24],["score",180,25],
    ["tile",32,24],["merges",9,26],["score",320,27],["tile",64,28],["merges",12,30],
    ["tile",32,24],["score",300,27],["tile",64,29],["merges",12,30],["tile",128,32],
    ["score",500,30],["tile",128,32],["merges",15,34],["score",760,35],["tile",256,38],
    ["tile",256,36],["merges",18,38],["score",1100,40],["tile",512,42],["score",1500,44],
    ["tile",512,42],["merges",22,44],["tile",1024,48],["score",2400,50],["tile",2048,56]
  ];
  function makeStart(index,blocked,target,type){const available=Array.from({length:16},(_,i)=>i).filter(i=>!blocked.has(i));const board=Array(16).fill(0);const chapter=Math.floor(index/5);const base=type==="tile"?Math.max(2,target/Math.pow(2,chapter>=4?4:chapter>=1?3:2)):2;const count=type==="tile"?Math.min(available.length,chapter>=4?Math.min(16,target/base):4):Math.min(8,available.length);const offset=(index*3)%available.length;for(let n=0;n<count;n++)board[available[(offset+n)%available.length]]=base;for(let n=count;n<Math.min(count+2,available.length);n++)board[available[(offset+n*3)%available.length]]=2;return board;}
  const levels=goals.map(([type,target,limit],index)=>{const blocked=new Set(masks[index]);return{index,type,target,limit,blocked,start:makeStart(index,blocked,target,type),seed:0x9e3779b9^(index+1)*2654435761};});

  const defaultSave={unlocked:1,cleared:{},stars:{},best:{}};let save;try{save={...defaultSave,...JSON.parse(localStorage.getItem(SAVE_KEY)||"{}")};}catch{save={...defaultSave};}
  let stageIndex=0,board=Array(16).fill(0),score=0,moves=0,merges=0,rngState=1,undoState=null,resultState="",startTime=0,hintUsed=false,restarts=0,swipeStart=null;
  const dom={loading:$("#loadingPanel"),loadingFill:$("#loadingFill"),mainGroup:$("#mainGroup"),main:$("#mainScreen"),guide:$(".game-page-info"),stage:$("#stageScreen"),battle:$("#battleScreen"),locale:$("#localeSelect"),mainProgress:$("#mainProgress"),start:$("#startBtn"),stageBack:$("#stageBackBtn"),rail:$("#stageRail"),stageSummary:$("#stageSummary"),chapterKicker:$("#chapterKicker"),chapterTitle:$("#chapterTitle"),chapterRule:$("#chapterRule"),battleBack:$("#battleBackBtn"),stageLabel:$("#stageLabel"),goalValue:$("#goalValue"),movesValue:$("#movesValue"),scoreValue:$("#scoreValue"),objective:$("#objectiveRow"),board:$("#gameBoard"),feedback:$("#moveFeedback"),undo:$("#undoBtn"),restart:$("#restartBtn"),hint:$("#hintBtn"),result:$("#resultPanel"),resultTitle:$("#resultTitle"),resultStars:$("#resultStars"),resultText:$("#resultText"),skillGrid:$("#skillGrid"),bestText:$("#bestText"),retry:$("#retryBtn"),resultStages:$("#resultStagesBtn"),next:$("#nextBtn"),leave:$("#leavePanel"),leaveText:$("#leaveText"),leaveContinue:$("#leaveContinueBtn"),leaveStages:$("#leaveStagesBtn"),tutorial:$("#tutorialPanel"),tutorialClose:$("#tutorialCloseBtn"),tutorialStart:$("#tutorialStartBtn")};
  function persist(){localStorage.setItem(SAVE_KEY,JSON.stringify(save));}
  function cloneState(){return{board:[...board],score,moves,merges,rngState};}
  function restoreState(state){board=[...state.board];score=state.score;moves=state.moves;merges=state.merges;rngState=state.rngState;renderBattle();}
  function rng(){rngState^=rngState<<13;rngState^=rngState>>>17;rngState^=rngState<<5;return(rngState>>>0)/4294967296;}
  function setScreen(name){document.body.dataset.screen=name;dom.mainGroup.hidden=name!=="main";dom.stage.hidden=name!=="stage";dom.battle.hidden=name!=="battle";}
  function chapter(index){return Math.min(5,Math.floor(index/5));}
  function animalFor(value){const tier=Math.max(1,Math.log2(value));return t(tileNames[Math.min(11,tier)]);}
  function goalText(level,objective=false){const left=Math.max(0,level.limit-moves);if(level.type==="tile")return t(objective?"objectiveTile":"goalTile",{animal:animalFor(level.target),value:level.target,left});if(level.type==="score")return t(objective?"objectiveScore":"goalScore",{target:level.target,left});return t(objective?"objectiveMerges":"goalMerges",{target:level.target,left});}
  function applyLocale(){document.documentElement.lang=locale;document.querySelectorAll("[data-i18n]").forEach(node=>node.textContent=t(node.dataset.i18n));dom.locale.value=locale;document.title=`${t("fullTitle")} — Internal Trial`;document.querySelector(".main-poster").alt=t("fullTitle");document.querySelector(".game-board").setAttribute("aria-label",t("fullTitle"));renderMainProgress();if(document.body.dataset.screen==="stage")renderStages();if(document.body.dataset.screen==="battle")renderBattle();}
  function renderMainProgress(){const cleared=Object.keys(save.cleared||{}).length,stars=Object.values(save.stars||{}).reduce((a,b)=>a+Number(b||0),0);dom.mainProgress.textContent=t("progress",{cleared,stars});}
  function showMain(){setScreen("main");renderMainProgress();}
  function renderStages(){dom.rail.innerHTML=levels.map((level,i)=>{const unlocked=i<(save.unlocked||1),clear=save.cleared?.[i],stars=save.stars?.[i]||0;return`<button class="stage-card ${unlocked?"unlocked":"locked"} ${i===stageIndex?"selected":""}" data-index="${i}" type="button" ${unlocked?"":"disabled"}><small><span>${t("mission")} ${i+1}</span><span>${"★".repeat(stars)}${"☆".repeat(Math.max(0,3-stars))}</span></small><strong>${goalText(level)}</strong><span>${clear?t("cleared"):unlocked?t("ready"):t("locked")}</span></button>`;}).join("");updateChapter(stageIndex);}
  function updateChapter(index){stageIndex=index;const ch=chapter(index);dom.chapterKicker.textContent=`${t("mission")} ${index+1} / 30`;dom.chapterTitle.textContent=t(`chapter${ch+1}`);dom.chapterRule.textContent=t(`rule${ch+1}`);dom.rail.querySelectorAll(".stage-card").forEach((card,i)=>card.classList.toggle("selected",i===index));}
  function showStage(focus=stageIndex){setScreen("stage");stageIndex=Math.min(focus,(save.unlocked||1)-1);renderStages();dom.stageSummary.textContent=t("progress",{cleared:Object.keys(save.cleared||{}).length,stars:Object.values(save.stars||{}).reduce((a,b)=>a+Number(b||0),0)});requestAnimationFrame(()=>dom.rail.querySelector(`[data-index="${stageIndex}"]`)?.scrollIntoView({behavior:"instant",inline:"center",block:"nearest"}));}
  function renderBoard(mergedIndices=[]){const level=levels[stageIndex];dom.board.innerHTML=Array.from({length:16},(_,i)=>{if(level.blocked.has(i))return`<div class="grid-cell is-blocked" data-index="${i}" aria-label="Stone"></div>`;const value=board[i],tier=value?Math.log2(value):0,asset=value?tileAssets[Math.min(10,tier-1)]:"";return`<div class="grid-cell" data-index="${i}">${value?`<div class="tile ${mergedIndices.includes(i)?"is-merged":""}" data-level="${tier}" aria-label="${animalFor(value)} ${value}"><img src="${asset}" alt="" draggable="false"/><span class="tile-value">${value}</span></div>`:""}</div>`;}).join("");}
  function renderBattle(mergedIndices=[]){const level=levels[stageIndex],left=Math.max(0,level.limit-moves);dom.stageLabel.textContent=`${t("mission")} ${stageIndex+1}`;dom.goalValue.textContent=goalText(level);dom.movesValue.textContent=`${moves}/${level.limit}`;dom.scoreValue.textContent=score;dom.objective.textContent=goalText(level,true);dom.undo.disabled=!undoState;renderBoard(mergedIndices);dom.next.hidden=stageIndex===29;dom.leaveText.textContent=t("leaveText",{stage:stageIndex+1});}
  function startStage(index){stageIndex=Math.max(0,Math.min(29,index));const level=levels[stageIndex];board=[...level.start];score=0;moves=0;merges=0;rngState=level.seed>>>0;undoState=null;resultState="";hintUsed=false;restarts=0;startTime=performance.now();dom.result.hidden=true;dom.leave.hidden=true;dom.feedback.textContent="";setScreen("battle");renderBattle();window.WonderAnalytics?.track?.("game_start",{gameId:"animal-2048",stage:stageIndex+1});requestAnimationFrame(()=>dom.board.focus({preventScroll:true}));}
  function getLines(direction){const lines=[];if(direction==="left"||direction==="right"){for(let r=0;r<4;r++){const line=[];for(let c=0;c<4;c++)line.push(r*4+c);if(direction==="right")line.reverse();lines.push(line);}}else{for(let c=0;c<4;c++){const line=[];for(let r=0;r<4;r++)line.push(r*4+c);if(direction==="down")line.reverse();lines.push(line);}}return lines;}
  function moveBoard(source,direction,blocked){const next=[...source],mergedIndices=[];let gained=0,mergeCount=0;for(const line of getLines(direction)){let segment=[];const flush=()=>{if(!segment.length)return;const values=segment.map(i=>source[i]).filter(Boolean),out=[];for(let i=0;i<values.length;i++){if(i+1<values.length&&values[i]===values[i+1]){const value=values[i]*2;out.push(value);gained+=value;mergeCount++;i++;}else out.push(values[i]);}while(out.length<segment.length)out.push(0);segment.forEach((cell,j)=>{next[cell]=out[j];if(out[j]&&j<values.length&&out[j]!==source[cell]&&out[j]>=4)mergedIndices.push(cell);});segment=[];};for(const cell of line){if(blocked.has(cell)){flush();next[cell]=0;}else segment.push(cell);}flush();}return{board:next,changed:next.some((v,i)=>v!==source[i]),gained,mergeCount,mergedIndices};}
  function spawnTile(){const level=levels[stageIndex],empty=board.map((value,i)=>!value&&!level.blocked.has(i)?i:-1).filter(i=>i>=0);if(!empty.length)return;const cell=empty[Math.floor(rng()*empty.length)],value=rng()<.88?2:4;board[cell]=value;}
  function achieved(){const level=levels[stageIndex];if(level.type==="tile")return board.some(value=>value>=level.target);if(level.type==="score")return score>=level.target;return merges>=level.target;}
  function canMove(){const level=levels[stageIndex];return["up","down","left","right"].some(direction=>moveBoard(board,direction,level.blocked).changed);}
  function performMove(direction){if(resultState||!dom.leave.hidden)return false;const level=levels[stageIndex],result=moveBoard(board,direction,level.blocked);if(!result.changed){dom.feedback.textContent=t("invalid");return false;}undoState=cloneState();board=result.board;score+=result.gained;moves++;merges+=result.mergeCount;spawnTile();dom.feedback.textContent=result.mergeCount?`+${result.gained}`:"";renderBattle(result.mergedIndices);window.WonderSound?.play?.("move");if(achieved())finish(true);else if(moves>=level.limit)finish(false,"moves");else if(!canMove())finish(false,"board");return true;}
  function finish(win,reason=""){resultState=win?"win":"lose";const level=levels[stageIndex],highest=Math.max(...board),empty=board.filter((value,i)=>!value&&!level.blocked.has(i)).length;if(win){const left=level.limit-moves,stars=hintUsed?2:left>=Math.ceil(level.limit*.45)&&restarts===0?3:2,previous=Number(save.best?.[stageIndex]||0),isBest=score>previous;save.cleared[stageIndex]=true;save.stars[stageIndex]=Math.max(save.stars?.[stageIndex]||0,stars);save.best[stageIndex]=Math.max(previous,score);save.unlocked=Math.max(save.unlocked||1,Math.min(30,stageIndex+2));persist();dom.resultTitle.textContent=t("winTitle");dom.resultStars.textContent="★".repeat(stars)+"☆".repeat(3-stars);dom.resultText.textContent=t("winText",{stage:stageIndex+1,moves,score});dom.bestText.textContent=isBest?t("newBest"):t("best",{score:save.best[stageIndex]});window.WonderAnalytics?.track?.("game_complete",{gameId:"animal-2048",stage:stageIndex+1,score,stars});}else{dom.resultTitle.textContent=t("loseTitle");dom.resultStars.textContent="";dom.resultText.textContent=t(reason==="moves"?"loseMoves":"loseBoard");dom.bestText.textContent=t("best",{score:save.best?.[stageIndex]||0});window.WonderAnalytics?.track?.("game_fail",{gameId:"animal-2048",stage:stageIndex+1,reason});}dom.skillGrid.innerHTML=`<div><small>${t("planning")}</small><strong>${t("planningValue",{moves})}</strong></div><div><small>${t("space")}</small><strong>${t("spaceValue",{empty})}</strong></div><div><small>${t("evolution")}</small><strong>${t("evolutionValue",{tier:Math.log2(highest||2)})}</strong></div>`;dom.next.hidden=!win||stageIndex===29;setTimeout(()=>{dom.result.hidden=false;dom.retry.focus({preventScroll:true});},260);}
  function undo(){if(!undoState){dom.feedback.textContent=t("undoEmpty");return;}const restore=undoState;undoState=null;restoreState(restore);dom.feedback.textContent=t("undoReady");}
  function restart(){restarts++;startStage(stageIndex);window.WonderAnalytics?.track?.("game_restart",{gameId:"animal-2048",stage:stageIndex+1});}
  function bestDirection(){const level=levels[stageIndex],directions=["up","left","right","down"],ranked=directions.map(direction=>{const result=moveBoard(board,direction,level.blocked);if(!result.changed)return{direction,score:-Infinity};const empty=result.board.filter((v,i)=>!v&&!level.blocked.has(i)).length,corner=Math.max(result.board[0],result.board[3],result.board[12],result.board[15]);return{direction,score:result.gained*4+empty*18+Math.log2(corner||2)*5};}).sort((a,b)=>b.score-a.score);return ranked[0]?.score>-Infinity?ranked[0].direction:null;}
  function showHint(){hintUsed=true;const direction=bestDirection();dom.feedback.textContent=direction?t("hintMove",{direction:t(direction)}):t("noHint");if(direction){const btn=$(`[data-direction="${direction}"]`);btn?.classList.add("is-hint");setTimeout(()=>btn?.classList.remove("is-hint"),1200);}}
  function closeTutorial(){localStorage.setItem(TUTORIAL_KEY,"1");dom.tutorial.hidden=true;}

  dom.start.addEventListener("click",()=>{showStage();if(!localStorage.getItem(TUTORIAL_KEY))dom.tutorial.hidden=false;});dom.stageBack.addEventListener("click",showMain);dom.rail.addEventListener("click",event=>{const card=event.target.closest(".stage-card.unlocked");if(card)startStage(Number(card.dataset.index));});dom.rail.addEventListener("wonder:stage-snap",event=>{const card=event.detail?.card;if(card)updateChapter(Number(card.dataset.index));});
  dom.battleBack.addEventListener("click",()=>{if(resultState){showStage(stageIndex);return;}dom.leave.hidden=false;dom.leaveContinue.focus({preventScroll:true});});dom.leaveContinue.addEventListener("click",()=>{dom.leave.hidden=true;dom.battleBack.focus({preventScroll:true});});dom.leaveStages.addEventListener("click",()=>{dom.leave.hidden=true;showStage(stageIndex);});
  dom.undo.addEventListener("click",undo);dom.restart.addEventListener("click",restart);dom.hint.addEventListener("click",showHint);document.querySelectorAll("[data-direction]").forEach(button=>button.addEventListener("click",()=>performMove(button.dataset.direction)));dom.retry.addEventListener("click",restart);dom.resultStages.addEventListener("click",()=>showStage(stageIndex));dom.next.addEventListener("click",()=>startStage(Math.min(29,stageIndex+1)));dom.tutorialClose.addEventListener("click",closeTutorial);dom.tutorialStart.addEventListener("click",()=>{closeTutorial();startStage(0);});
  dom.locale.addEventListener("change",()=>{const next=dom.locale.value;if(!STRINGS[next])return;if(/^https?:$/.test(location.protocol)&&SEGMENTS[next]&&LOCALE_SEGMENTS[firstSegment]){location.href=`/${SEGMENTS[next]}/games/animal-2048/${location.search}${location.hash}`;return;}locale=next;localStorage.setItem("weightPlayLocale",locale);applyLocale();});
  dom.board.addEventListener("pointerdown",event=>{swipeStart={x:event.clientX,y:event.clientY,id:event.pointerId};dom.board.setPointerCapture?.(event.pointerId);});dom.board.addEventListener("pointerup",event=>{if(!swipeStart||swipeStart.id!==event.pointerId)return;const dx=event.clientX-swipeStart.x,dy=event.clientY-swipeStart.y;swipeStart=null;if(Math.max(Math.abs(dx),Math.abs(dy))<24)return;performMove(Math.abs(dx)>Math.abs(dy)?dx>0?"right":"left":dy>0?"down":"up");});dom.board.addEventListener("pointercancel",()=>{swipeStart=null;});
  document.addEventListener("keydown",event=>{if(!dom.leave.hidden){if(event.key==="Escape"){event.preventDefault();dom.leaveContinue.click();}return;}if(document.body.dataset.screen!=="battle"||!dom.result.hidden)return;const map={ArrowUp:"up",w:"up",W:"up",ArrowDown:"down",s:"down",S:"down",ArrowLeft:"left",a:"left",A:"left",ArrowRight:"right",d:"right",D:"right"};if(map[event.key]){event.preventDefault();performMove(map[event.key]);}else if(event.key==="u"||event.key==="U"){event.preventDefault();undo();}else if(event.key==="r"||event.key==="R"){event.preventDefault();restart();}else if(event.key==="h"||event.key==="H"){event.preventDefault();showHint();}});
  window.__animal2048Smoke={levels:levels.map(level=>({type:level.type,target:level.target,limit:level.limit,blocked:[...level.blocked],start:[...level.start]})),startStage,performMove,snapshot:()=>({screen:document.body.dataset.screen,stage:stageIndex+1,board:[...board],score,moves,merges,rngState,undo:Boolean(undoState),result:resultState,save:JSON.parse(JSON.stringify(save))}),forceResult:(win,reason)=>finish(Boolean(win),reason||"moves"),bestDirection};
  applyLocale();const assets=["../../assets/animal-2048-cover.webp","../../assets/animal-2048-background.webp",...tileAssets,"../../assets/weightplay-character-moon-cap-owl-cutout.webp"];let loaded=0;Promise.allSettled(assets.map(src=>new Promise(resolve=>{const img=new Image(),done=()=>{loaded++;dom.loadingFill.style.width=`${loaded/assets.length*100}%`;resolve();};img.onload=done;img.onerror=done;img.src=src;}))).then(()=>setTimeout(()=>{dom.loading.hidden=true;showMain();window.WonderAnalytics?.track?.("game_view",{gameId:"animal-2048"});},180));
})();
