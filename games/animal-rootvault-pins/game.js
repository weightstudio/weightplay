(function(){
"use strict";
const $=id=>document.getElementById(id),LOCALES=window.ROOTVAULT_PIN_LOCALES||{},CODES=["en","zh-Hant","zh-Hans","ja","ko","es","pt-BR","fr","de","it","ru","hi","ar"],LABELS={en:"English","zh-Hant":"繁體中文","zh-Hans":"简体中文",ja:"日本語",ko:"한국어",es:"Español","pt-BR":"Português",fr:"Français",de:"Deutsch",it:"Italiano",ru:"Русский",hi:"हिन्दी",ar:"العربية"},STORE="weightplay:animal-rootvault-pins:v1",memory=new Map();
const els={loading:$("loadingPanel"),mainGroup:$("mainGroup"),stageScreen:$("stageScreen"),battleScreen:$("battleScreen"),localeSelect:$("localeSelect"),mainProgress:$("mainProgress"),start:$("start"),stageBack:$("stageBack"),stageRail:$("stageRail"),stageSummary:$("stageSummary"),briefTab:$("briefTab"),workshopTab:$("workshopTab"),briefPanel:$("briefPanel"),workshopPanel:$("workshopPanel"),selectedNumber:$("selectedNumber"),selectedName:$("selectedName"),selectedRule:$("selectedRule"),enterStage:$("enterStage"),seedCount:$("seedCount"),workshopHint:$("workshopHint"),upgradeList:$("upgradeList"),battleLive:$("battleLive"),battleBack:$("battleBack"),battleChapter:$("battleChapter"),battleStage:$("battleStage"),pullCount:$("pullCount"),battleHelp:$("battleHelp"),ruleCue:$("ruleCue"),board:$("chamberBoard"),feedback:$("feedback"),restart:$("restart"),pullPin:$("pullPin"),helpModal:$("helpModal"),helpClose:$("helpClose"),leaveModal:$("leaveModal"),leaveContinue:$("leaveContinue"),leaveStage:$("leaveStage"),resultModal:$("resultModal"),resultCard:document.querySelector(".result-card"),resultKicker:$("resultKicker"),resultTitle:$("resultTitle"),resultText:$("resultText"),resultPulls:$("resultPulls"),resultBest:$("resultBest"),resultReward:$("resultReward"),retry:$("retry"),resultStages:$("resultStages"),next:$("next")};
const storage={get(key){try{return localStorage.getItem(key)}catch{return memory.get(key)||null}},set(key,value){try{localStorage.setItem(key,value)}catch{memory.set(key,value)}}};
const defaultSave=()=>({unlocked:1,completed:{},best:{},seeds:0,upgrades:[0,0,0],tutorial:false});
function normalizeSave(raw){const value=raw&&typeof raw==="object"?raw:{};return{unlocked:Math.max(1,Math.min(30,Number(value.unlocked)||1)),completed:value.completed&&typeof value.completed==="object"?value.completed:{},best:value.best&&typeof value.best==="object"?value.best:{},seeds:Math.max(0,Number(value.seeds)||0),upgrades:[0,1,2].map(i=>Math.max(0,Math.min(3,Number(value.upgrades?.[i])||0))),tutorial:Boolean(value.tutorial)}}
let save=(()=>{try{return normalizeSave(JSON.parse(storage.get(STORE)||"null"))}catch{return defaultSave()}})(),locale="en",screen="main",stageIndex=0,run=null,selectedPin=null,busy=false,modalOpen=false,lastSelectedHint=null,stageMarkFrame=0,boardLayoutFrame=0,pullTimer=0,pullDeadline=0,pullRemaining=0,pendingPullResult=null,windowFocused=document.hasFocus();
function persist(){storage.set(STORE,JSON.stringify(save))}
function t(key,data={}){let value=LOCALES[locale]?.[key]??LOCALES.en?.[key]??key;const translate=window.WeightPlayGameRuntimeLocalizer?.translate;if(locale!=="en"&&!LOCALES[locale]&&translate)value=Array.isArray(value)?value.map(item=>translate(String(item))):translate(String(value));if(Array.isArray(value))return value;return String(value).replace(/\{(\w+)\}/g,(_,name)=>data[name]??`{${name}}`)}
function track(name,data={}){try{window.WeightPlayAnalytics?.track?.(name,{game:"animal-rootvault-pins",stage:stageIndex+1,...data})}catch{}}
function transformPoint(point,variant){const [x,y]=point;return[[x,y],[1-x,y],[x,1-y],[1-x,1-y],[y,1-x]][variant]}
function createBuilder(index,chapter,variant){const rooms=[],pins=[],solution=[];let counter=0;const addRoom=(id,x,y,contents=[],type="")=>{rooms.push({id,x,y,contents:[...contents],type,lock:type==="lock"?"rune":""});return id};const addPin=(from,to,{required=true,lock="",id=""}={})=>{const pinId=id||`p${chapter}${variant}${counter++}`;pins.push({id:pinId,from,to,lock});if(required)solution.push(pinId);return pinId};const route=(from,to,prefix,relay)=>{if(relay){addPin(from,relay,{id:`${prefix}a`});addPin(relay,to,{id:`${prefix}b`})}else addPin(from,to,{id:prefix})};const finish=()=>{rooms.forEach(room=>{[room.x,room.y]=transformPoint([room.x,room.y],variant)});return{index,chapter,variant,rooms,pins,solution,par:solution.length}};return{addRoom,addPin,route,finish,rooms,pins,solution}}
function buildLevel(index){const chapter=Math.floor(index/5),v=index%5,b=createBuilder(index,chapter,v),r=b.addRoom,p=b.addPin,route=b.route;const relayA=v>=2,relayC=v===1||v>=3;
  if(chapter===0){r("A",.16,.2,["taro"]);r("C",.82,.2,["core"]);r("S",.5,.82,[],"sanctuary");r("V",.88,.72,[],"void");if(relayA)r("MA",.22,.52);if(relayC)r("MC",.75,.52);route("A","S","a",relayA?"MA":"");route("C","S","c",relayC?"MC":"");if(v>=1)p("A","V",{required:false,id:"d1"});if(v>=4)p("C","V",{required:false,id:"d2"})}
  if(chapter===1){r("W",.12,.16,["water"]);r("E",.46,.28,["ember"]);r("A",.86,.16,["taro"]);r("C",.83,.72,["core"]);r("S",.47,.84,[],"sanctuary");r("V",.12,.74,[],"void");if(relayA)r("MA",.7,.48);if(relayC)r("MC",.68,.68);p("W","E",{id:"water"});route("A","E","taro",relayA?"MA":"");p("E","S",{id:"safe"});route("C","S","core",relayC?"MC":"");if(v>=1)p("A","V",{required:false,id:"danger"});if(v>=4)p("C","V",{required:false,id:"d2"})}
  if(chapter===2){r("H",.1,.18,["ward"]);r("A",.48,.18,["taro"]);r("X",.48,.54,["shadow"]);r("C",.84,.22,["core"]);r("S",.48,.84,[],"sanctuary");r("V",.86,.73,[],"void");if(relayA)r("MA",.28,.42);if(relayC)r("MC",.75,.55);p("H","A",{id:"ward"});route("A","X","taro",relayA?"MA":"");p("X","S",{id:"shadow"});route("C","S","core",relayC?"MC":"");if(v>=1)p("A","V",{required:false,id:"danger"});if(v>=4)p("C","X",{required:false,id:"d2"})}
  if(chapter===3){r("W",.1,.12,["water"]);r("E",.33,.28,["ember"]);r("H",.62,.1,["ward"]);r("A",.86,.2,["taro"]);r("X",.54,.58,["shadow"]);r("C",.85,.7,["core"]);r("S",.44,.86,[],"sanctuary");r("V",.1,.72,[],"void");if(relayA)r("MA",.69,.4);if(relayC)r("MC",.7,.78);p("W","E",{id:"water"});p("H","A",{id:"ward"});route("A","E","taro",relayA?"MA":"");p("E","X",{id:"ember"});p("X","S",{id:"shadow"});route("C","S","core",relayC?"MC":"");if(v>=1)p("A","X",{required:false,id:"danger"});if(v>=4)p("C","X",{required:false,id:"d2"})}
  if(chapter===4){r("K",.12,.16,["key"]);r("L",.42,.34,["lock"],"lock");r("A",.84,.15,["taro"]);r("C",.82,.72,["core"]);r("S",.42,.84,[],"sanctuary");r("V",.12,.73,[],"void");if(relayA)r("MA",.68,.43);if(relayC)r("MC",.67,.67);p("K","L",{id:"key"});if(relayA){p("A","MA",{id:"taroa",lock:"rune"});p("MA","L",{id:"tarob"})}else p("A","L",{id:"taro",lock:"rune"});p("L","S",{id:"lockway"});route("C","S","core",relayC?"MC":"");if(v>=1)p("K","V",{required:false,id:"d1"});if(v>=4)p("C","V",{required:false,id:"d2"})}
  if(chapter===5){r("W",.08,.1,["water"]);r("E",.3,.3,["ember"]);r("H",.56,.08,["ward"]);r("A",.86,.16,["taro"]);r("K",.09,.52,["key"]);r("L",.36,.56,["lock"],"lock");r("X",.62,.55,["shadow"]);r("C",.88,.68,["core"]);r("S",.5,.87,[],"sanctuary");r("V",.12,.82,[],"void");p("W","E",{id:"water"});p("H","A",{id:"ward"});p("K","L",{id:"key"});p("A","E",{id:"taro",lock:"rune"});p("E","X",{id:"ember"});p("X","S",{id:"shadow"});p("C","S",{id:"core"});if(v>=1)p("A","X",{required:false,id:"d1"});if(v>=2)p("C","X",{required:false,id:"d2"});if(v>=3)p("W","S",{required:false,id:"d3"});if(v>=4)p("K","V",{required:false,id:"d4"})}
  return b.finish()}
const levels=Array.from({length:30},(_,index)=>buildLevel(index));
function createRun(level){return{level,rooms:Object.fromEntries(level.rooms.map(room=>[room.id,{...room,contents:[...room.contents]}])),pins:Object.fromEntries(level.pins.map(pin=>[pin.id,{...pin,pulled:false}])),unlocked:new Set(),armor:false,pulls:0,ended:false,result:null,events:[]}}
function removeOne(contents,type){const index=contents.indexOf(type);if(index<0)return false;contents.splice(index,1);return true}
function resolveRoom(state,room){const events=[];if(room.contents.includes("key")&&room.contents.includes("lock")){removeOne(room.contents,"key");removeOne(room.contents,"lock");state.unlocked.add(room.lock||"rune");events.push("unlocked")}
  while(room.contents.includes("water")&&room.contents.includes("ember")){removeOne(room.contents,"water");removeOne(room.contents,"ember");events.push("steam")}
  if(room.contents.includes("taro")&&room.contents.includes("ward")){removeOne(room.contents,"ward");state.armor=true;events.push("warded")}
  if(room.contents.includes("taro")&&room.contents.includes("ember")){state.ended=true;state.result={win:false,reason:"failTaro"}}
  if(!state.ended&&room.contents.includes("taro")&&room.contents.includes("shadow")){if(state.armor){removeOne(room.contents,"shadow");state.armor=false;events.push("warded")}else{state.ended=true;state.result={win:false,reason:"failTaro"}}}
  if(!state.ended&&room.contents.includes("core")&&room.contents.includes("shadow")){state.ended=true;state.result={win:false,reason:"failCore"}}
  if(!state.ended&&room.type==="void"&&(room.contents.includes("taro")||room.contents.includes("core"))){state.ended=true;state.result={win:false,reason:room.contents.includes("taro")?"failTaro":"failCore"}}
  if(!state.ended&&room.type==="sanctuary"&&room.contents.includes("taro")&&room.contents.includes("core")){state.ended=true;state.result={win:true,reason:"win"}}
  state.events=events;return events}
function applyPin(state,pinId){const pin=state.pins[pinId];if(!pin||pin.pulled||state.ended)return{ok:false,reason:"unavailable",events:[]};if(pin.lock&&!state.unlocked.has(pin.lock))return{ok:false,reason:"lockedPin",events:[]};pin.pulled=true;state.pulls++;const source=state.rooms[pin.from],target=state.rooms[pin.to],moved=[...source.contents];source.contents.length=0;target.contents.push(...moved);const events=resolveRoom(state,target);return{ok:true,moved,source,target,events,empty:moved.length===0}}
function simulateSolution(level){const state=createRun(level);for(const pinId of level.solution){const result=applyPin(state,pinId);if(!result.ok)return{pass:false,state,pinId,reason:result.reason}}return{pass:Boolean(state.ended&&state.result?.win),state}}
function initLocale(){const path=location.pathname.split("/").filter(Boolean)[0],routeMap={en:"en","zh-tw":"zh-Hant","zh-cn":"zh-Hans",ja:"ja",ko:"ko",es:"es","pt-br":"pt-BR",fr:"fr",de:"de",it:"it",ru:"ru",hi:"hi",ar:"ar"},saved=storage.get("weightplay:animal-rootvault-pins:locale")||storage.get("weightplay:locale");locale=routeMap[path]||(CODES.includes(saved)?saved:"en");if(!CODES.includes(locale))locale="en";els.localeSelect.innerHTML=CODES.map(code=>`<option value="${code}">${LABELS[code]}</option>`).join("");els.localeSelect.value=locale}
function applyLocale(){document.documentElement.lang=locale;document.documentElement.dir=locale==="ar"?"rtl":"ltr";document.title=`${t("title")} | Internal WeightPlay Trial`;document.querySelectorAll("[data-t]").forEach(node=>node.textContent=t(node.dataset.t));document.querySelectorAll("[data-t-aria]").forEach(node=>node.setAttribute("aria-label",t(node.dataset.tAria)));document.querySelectorAll("[data-t-alt]").forEach(node=>node.setAttribute("alt",t(node.dataset.tAlt)));renderMain();renderStages();renderBrief();renderWorkshop();if(run)renderBattleHeader()}
function renderMain(){const done=Object.keys(save.completed).filter(key=>save.completed[key]).length;els.mainProgress.textContent=t("progress",{done})}
function setScreen(next){screen=next;document.body.dataset.screen=next;els.mainGroup.hidden=next!=="main";els.stageScreen.hidden=next!=="stage";els.battleScreen.hidden=next!=="battle";if(next==="stage"){renderStages();renderBrief();renderWorkshop();requestAnimationFrame(centerStage)}if(next==="battle")window.dispatchEvent(new Event("weightplay:battle-open"))}
function stageName(index){const level=levels[index];return`${t("chapterNames")[level.chapter]} · ${index+1}`}
function renderStages(){els.stageRail.innerHTML="";for(const level of levels){const locked=level.index+1>save.unlocked,card=document.createElement("button");card.type="button";card.className=`stage-card${locked?" locked":""}`;card.disabled=locked;card.dataset.stageIndex=String(level.index);card.dataset.index=String(level.index);if(level.index===Math.max(0,save.unlocked-1))card.dataset.wpStageRecommended="true";card.innerHTML=`<small>${t("chapter",{n:level.chapter+1})}</small><strong>${stageName(level.index)}</strong><span>${locked?t("locked"):t("pulls")} · ${level.par}</span><small>${t("chapterRules")[level.chapter]}</small>`;card.addEventListener("click",()=>{if(!locked){stageIndex=level.index;renderBrief();startBattle(stageIndex)}});els.stageRail.append(card)}els.stageSummary.textContent=`${save.unlocked} / 30 · ${Object.keys(save.completed).length}`;requestAnimationFrame(markCenteredStage)}
function markCenteredStage(){stageMarkFrame=0;const cards=[...els.stageRail.querySelectorAll(".stage-card")];if(!cards.length||!els.stageRail.getClientRects().length)return;const rail=els.stageRail.getBoundingClientRect(),center=(rail.left+rail.right)/2;let chosen=cards[0],distance=Infinity;for(const card of cards){const rect=card.getBoundingClientRect(),next=Math.abs((rect.left+rect.right)/2-center);if(next<distance){distance=next;chosen=card}}cards.forEach(card=>{const active=card===chosen;card.classList.toggle("centered",active);if(active)card.setAttribute("aria-current","true");else card.removeAttribute("aria-current")});stageIndex=Number(chosen.dataset.stageIndex);renderBrief()}
function queueStageMark(){if(stageMarkFrame)return;stageMarkFrame=requestAnimationFrame(markCenteredStage)}
function centerStage(){const target=Math.max(0,Math.min(29,stageIndex||save.unlocked-1)),card=els.stageRail.querySelector(`[data-stage-index="${target}"]`);card?.scrollIntoView({inline:"center",block:"nearest",behavior:"instant"});stageIndex=target;renderBrief();requestAnimationFrame(markCenteredStage)}
function renderBrief(){const level=levels[stageIndex];els.selectedNumber.textContent=String(stageIndex+1);els.selectedName.textContent=stageName(stageIndex);els.selectedRule.textContent=t("chapterRules")[level.chapter];els.enterStage.disabled=stageIndex+1>save.unlocked}
function setStageTab(tab){const brief=tab==="brief";els.briefTab.classList.toggle("active",brief);els.workshopTab.classList.toggle("active",!brief);els.briefPanel.hidden=!brief;els.workshopPanel.hidden=brief;if(!brief)els.workshopHint.textContent=t("workshopReady")}
function upgradeCost(level){return 4+level*4}
function renderWorkshop(){els.seedCount.textContent=t("seeds",{n:save.seeds});els.upgradeList.innerHTML="";for(let i=0;i<3;i++){const level=save.upgrades[i],cost=upgradeCost(level),button=document.createElement("button");button.type="button";button.className="upgrade";button.disabled=level>=3;button.innerHTML=`<strong>${t("upgradeNames")[i]}</strong><small>${t("upgradeDescs")[i]}</small><b>${t("level",{n:level})}</b><span>${level>=3?t("maxed"):t("upgradeBuy",{n:cost})}</span>`;button.addEventListener("click",()=>{if(level>=3)return;if(save.seeds<cost){els.workshopHint.textContent=t("needSeeds",{cost,n:save.seeds});return}save.seeds-=cost;save.upgrades[i]++;persist();renderWorkshop();els.workshopHint.textContent=t("upgradeComplete",{name:t("upgradeNames")[i],level:save.upgrades[i]})});els.upgradeList.append(button)}}
function renderBattleHeader(){const level=levels[stageIndex];els.battleChapter.textContent=t("chapter",{n:level.chapter+1});els.battleStage.textContent=stageName(stageIndex);els.pullCount.textContent=String(run?.pulls||0);els.ruleCue.textContent=`${t("chapterRules")[level.chapter]}${save.upgrades[2]>0&&level.chapter>=2?" · 🛡":""}`}
function roomCenter(room){return{x:room.x*els.board.clientWidth,y:room.y*els.board.clientHeight}}
function pieceNode(type){const node=document.createElement("span");node.className=`piece ${type}${type==="taro"&&run?.armor?" armored":""}`;node.dataset.piece=type;node.setAttribute("aria-hidden","true");return node}
function renderBoard(){if(!run)return;els.board.innerHTML="";for(const pin of Object.values(run.pins)){const from=run.rooms[pin.from],to=run.rooms[pin.to],a=roomCenter(from),b=roomCenter(to),dx=b.x-a.x,dy=b.y-a.y,length=Math.hypot(dx,dy),angle=Math.atan2(dy,dx)*180/Math.PI,channel=document.createElement("span");channel.className="channel";channel.style.cssText=`left:${a.x}px;top:${a.y}px;width:${length}px;transform:rotate(${angle}deg)`;els.board.append(channel)}
  for(const room of Object.values(run.rooms)){const node=document.createElement("div");node.className=`room ${room.type||""}`;node.dataset.room=room.id;node.style.left=`${room.x*100}%`;node.style.top=`${room.y*100}%`;const label=document.createElement("small");label.className="room-label";label.textContent=room.type==="sanctuary"?"✦":room.type==="void"?"◆":"";const pieces=document.createElement("div");pieces.className="pieces";room.contents.forEach(type=>pieces.append(pieceNode(type)));if(room.type==="sanctuary"&&!room.contents.includes("sanctuary"))pieces.prepend(pieceNode("sanctuary"));if(room.type==="void"&&!room.contents.includes("void"))pieces.prepend(pieceNode("void"));node.append(label,pieces);els.board.append(node)}
  const hint=save.upgrades[0]>0?run.level.solution.find(id=>!run.pins[id].pulled):"";for(const pin of Object.values(run.pins)){const from=run.rooms[pin.from],to=run.rooms[pin.to],a=roomCenter(from),b=roomCenter(to),button=document.createElement("button");button.type="button";button.className=`pin${pin.pulled?" pulled":""}${pin.lock&&!run.unlocked.has(pin.lock)?" locked":""}${pin.id===selectedPin?" selected":""}`;if(hint===pin.id)button.dataset.safeHint="true";button.disabled=pin.pulled||busy;button.dataset.pin=pin.id;button.style.left=`${(a.x+b.x)/2}px`;button.style.top=`${(a.y+b.y)/2}px`;button.setAttribute("aria-label",`${t("pullPin")} ${pin.from} → ${pin.to}${pin.lock&&!run.unlocked.has(pin.lock)?` · ${t("locked")}`:""}`);const art=document.createElement("span");art.className="pin-art";button.append(art);button.addEventListener("click",()=>selectPin(pin.id));button.addEventListener("keydown",event=>{if((event.code==="Enter"||event.code==="Space")&&!event.repeat){event.preventDefault();selectPin(pin.id);pullSelected()}});els.board.append(button)}updatePullButton()}
function queueBoardLayout(){if(boardLayoutFrame)cancelAnimationFrame(boardLayoutFrame);boardLayoutFrame=requestAnimationFrame(()=>{boardLayoutFrame=requestAnimationFrame(()=>{boardLayoutFrame=0;if(screen==="battle"&&run&&!busy)renderBoard()})})}
function updatePullButton(){const pin=run?.pins[selectedPin];els.pullPin.disabled=modalOpen||busy||!pin||pin.pulled||run?.ended;els.pullPin.textContent=pin?.lock&&!run.unlocked.has(pin.lock)?t("locked"):t("pullPin")}
function selectPin(pinId){if(!run||modalOpen||busy||run.ended)return false;selectedPin=pinId;lastSelectedHint=pinId;const pin=run.pins[pinId];els.feedback.textContent=pin.lock&&!run.unlocked.has(pin.lock)?t("lockedPin"):t("selected");renderBoard();return true}
function addEffect(room,type){const effect=document.createElement("span"),center=roomCenter(room);effect.className=`effect ${type}`;effect.style.left=`${center.x}px`;effect.style.top=`${center.y}px`;els.board.append(effect);setTimeout(()=>effect.remove(),700)}
function eventFeedback(events,empty){if(empty)return t("empty");if(events.includes("unlocked"))return t("unlocked");if(events.includes("steam"))return t("steam");if(events.includes("warded"))return t("warded");return t("pulled")}
function animateMove(result){const sourceNode=els.board.querySelector(`[data-room="${result.source.id}"]`),pieces=[...sourceNode.querySelectorAll(".piece[data-piece]")],from=roomCenter(result.source),to=roomCenter(result.target);sourceNode.querySelector(".pieces").style.opacity="0";for(const piece of pieces){const clone=piece.cloneNode(true);clone.classList.add("moving-piece");clone.style.left=`${from.x-21}px`;clone.style.top=`${from.y-21}px`;els.board.append(clone);requestAnimationFrame(()=>clone.style.transform=`translate(${to.x-from.x}px,${to.y-from.y}px)`)}}
function clearPullCompletion(){if(pullTimer)clearTimeout(pullTimer);pullTimer=0;pullDeadline=0;pullRemaining=0;pendingPullResult=null}
function finishPullCompletion(){if(!pendingPullResult)return;const result=pendingPullResult;pullTimer=0;pullDeadline=0;pullRemaining=0;pendingPullResult=null;busy=false;renderBoard();const effectType=result.events.includes("steam")?"":result.events.includes("warded")?"ward":result.events.includes("unlocked")?"ward":"";if(result.events.includes("steam"))addEffect(result.target,"");else if(effectType)addEffect(result.target,effectType);els.feedback.textContent=eventFeedback(result.events,result.empty);selectedPin=null;updatePullButton();if(run?.ended)finishResult(run.result.win,run.result.reason)}
function schedulePullCompletion(delay=pullRemaining||520){if(!pendingPullResult||pullTimer||document.hidden||!windowFocused)return;pullRemaining=Math.max(0,delay);pullDeadline=performance.now()+pullRemaining;pullTimer=setTimeout(finishPullCompletion,pullRemaining)}
function suspendPullCompletion(){if(!pendingPullResult||!pullTimer)return;pullRemaining=Math.max(0,pullDeadline-performance.now());clearTimeout(pullTimer);pullTimer=0;pullDeadline=0}
function resumePullCompletion(){if(document.hidden||!windowFocused)return;schedulePullCompletion()}
function pullSelected(){if(!run||modalOpen||busy||run.ended||!selectedPin)return false;const pin=run.pins[selectedPin];if(pin.lock&&!run.unlocked.has(pin.lock)){els.feedback.textContent=t("lockedPin");return false}const result=applyPin(run,selectedPin);if(!result.ok){els.feedback.textContent=t(result.reason);return false}busy=true;pendingPullResult=result;pullRemaining=520;renderBattleHeader();animateMove(result);els.feedback.textContent=t("pulled");schedulePullCompletion();track("rootvault_pin_pull",{pin:selectedPin});return true}
function setBattleLiveCovered(covered){els.battleLive.inert=covered;if(covered)els.battleLive.setAttribute("aria-hidden","true");else els.battleLive.removeAttribute("aria-hidden")}
function restartRun(){if(!run)return;clearPullCompletion();const keep=save.upgrades[1]>0?lastSelectedHint:null;run=createRun(levels[stageIndex]);selectedPin=keep&&run.pins[keep]?keep:null;busy=false;modalOpen=false;els.resultModal.hidden=true;els.helpModal.hidden=true;els.leaveModal.hidden=true;setBattleLiveCovered(false);renderBattleHeader();renderBoard();els.feedback.textContent=t("ready")}
function startBattle(index,{skipTutorial=false}={}){clearPullCompletion();stageIndex=Math.max(0,Math.min(29,index));run=createRun(levels[stageIndex]);selectedPin=null;busy=false;lastSelectedHint=null;modalOpen=false;els.resultModal.hidden=true;els.leaveModal.hidden=true;els.helpModal.hidden=true;setBattleLiveCovered(false);setScreen("battle");renderBattleHeader();renderBoard();queueBoardLayout();els.feedback.textContent=t("ready");track("game_start");if(!save.tutorial&&!skipTutorial){modalOpen=true;setBattleLiveCovered(true);els.helpModal.hidden=false;els.helpClose.focus({preventScroll:true})}}
function finishResult(win,reason){if(!run)return;modalOpen=true;const reward=win?Math.max(1,4-Math.max(0,run.pulls-run.level.par)):0;if(win){const key=String(stageIndex);save.completed[key]=true;const prior=Number(save.best[key]||0);if(!prior||run.pulls<prior)save.best[key]=run.pulls;save.unlocked=Math.max(save.unlocked,Math.min(30,stageIndex+2));save.seeds+=reward;persist();els.resultKicker.textContent=t("winKicker");els.resultTitle.textContent=t("winTitle");els.resultText.textContent=t("winText",{n:stageIndex+1});els.resultCard.classList.remove("fail")}else{els.resultKicker.textContent=t("failKicker");els.resultTitle.textContent=t("failTitle");els.resultText.textContent=t(reason||"failText");els.resultCard.classList.add("fail")}els.resultPulls.textContent=String(run.pulls);els.resultBest.textContent=save.best[stageIndex]?t("bestPulls",{n:save.best[stageIndex]}):"—";els.resultReward.textContent=win?t("seedReward",{n:reward}):"—";els.next.hidden=!win||stageIndex>=29;els.resultModal.hidden=false;setBattleLiveCovered(true);(win&&stageIndex<29?els.next:els.retry).focus({preventScroll:true});track(win?"game_complete":"game_fail",{pulls:run.pulls})}
function openHelp(){if(!run||run.ended||busy)return;modalOpen=true;setBattleLiveCovered(true);els.helpModal.hidden=false;els.helpClose.focus({preventScroll:true})}
function closeHelp(){modalOpen=false;els.helpModal.hidden=true;setBattleLiveCovered(false);save.tutorial=true;persist();els.board.focus({preventScroll:true})}
function openLeave(){if(!run||run.ended){setScreen("stage");return}if(busy)return;modalOpen=true;setBattleLiveCovered(true);els.leaveModal.hidden=false;els.leaveContinue.focus({preventScroll:true})}
function continueRun(){modalOpen=false;els.leaveModal.hidden=true;setBattleLiveCovered(false);els.board.focus({preventScroll:true})}
function trapModalFocus(modal,event){if(event.key!=="Tab")return;const buttons=[...modal.querySelectorAll("button:not([hidden]):not([disabled])")];if(!buttons.length)return;const first=buttons[0],last=buttons.at(-1),active=document.activeElement;if(buttons.length===1||event.shiftKey&&(active===first||!modal.contains(active))||!event.shiftKey&&active===last){event.preventDefault();(event.shiftKey?last:first).focus({preventScroll:true})}}
for(const modal of [els.helpModal,els.leaveModal,els.resultModal]){modal.setAttribute("role","dialog");modal.setAttribute("aria-modal","true");modal.addEventListener("keydown",event=>trapModalFocus(modal,event))}
els.localeSelect.addEventListener("change",()=>{locale=CODES.includes(els.localeSelect.value)?els.localeSelect.value:"en";storage.set("weightplay:animal-rootvault-pins:locale",locale);storage.set("weightplay:locale",locale);window.WonderI18n?.setLocale?.(locale);if(!LOCALES[locale]&&window.WeightPlayGameRuntimeLocalizer?.locale!==locale){window.location.reload();return}applyLocale()});
els.start.addEventListener("click",()=>{stageIndex=Math.max(0,save.unlocked-1);setScreen("stage")});els.stageBack.addEventListener("click",()=>setScreen("main"));els.briefTab.addEventListener("click",()=>setStageTab("brief"));els.workshopTab.addEventListener("click",()=>setStageTab("workshop"));els.enterStage.addEventListener("click",()=>startBattle(stageIndex));els.stageRail.addEventListener("scroll",queueStageMark,{passive:true});els.stageRail.addEventListener("wonder:stage-snap",event=>{const index=Number(event.detail?.index);if(Number.isInteger(index)&&index>=0&&index<30)stageIndex=index;markCenteredStage()});els.battleBack.addEventListener("click",openLeave);els.pullPin.addEventListener("click",pullSelected);els.restart.addEventListener("click",restartRun);els.battleHelp.addEventListener("click",openHelp);els.helpClose.addEventListener("click",closeHelp);els.leaveContinue.addEventListener("click",continueRun);els.leaveStage.addEventListener("click",()=>{modalOpen=false;els.leaveModal.hidden=true;run=null;setScreen("stage")});els.retry.addEventListener("click",restartRun);els.resultStages.addEventListener("click",()=>{run=null;setScreen("stage")});els.next.addEventListener("click",()=>startBattle(Math.min(29,stageIndex+1),{skipTutorial:true}));
window.addEventListener("keydown",event=>{if(screen!=="battle"||!run)return;if(event.code==="Escape"){if(!els.helpModal.hidden)closeHelp();else if(!els.leaveModal.hidden)continueRun();else if(els.resultModal.hidden)openLeave()}if((event.code==="Enter"||event.code==="Space")&&!event.repeat&&document.activeElement===els.board&&selectedPin){event.preventDefault();pullSelected()}});
window.addEventListener("resize",()=>{if(screen==="battle"&&run&&!busy)queueBoardLayout()},{passive:true});
window.addEventListener("blur",()=>{windowFocused=false;suspendPullCompletion()});
window.addEventListener("focus",()=>{windowFocused=true;resumePullCompletion()});
window.addEventListener("pagehide",suspendPullCompletion);
window.addEventListener("pageshow",resumePullCompletion);
document.addEventListener("visibilitychange",()=>{if(document.hidden)suspendPullCompletion();else resumePullCompletion()});
window.__animalRootvaultPinsSmoke={levels,simulateSolution,startBattle:(index=0)=>startBattle(index,{skipTutorial:true}),snapshot:()=>run?{stage:stageIndex,pulls:run.pulls,ended:run.ended,result:run.result,selectedPin,busy,rooms:Object.fromEntries(Object.entries(run.rooms).map(([id,room])=>[id,[...room.contents]])),pins:Object.values(run.pins).map(pin=>({id:pin.id,lock:pin.lock,pulled:pin.pulled})),unlocked:[...run.unlocked]}:null,select:selectPin,pull:pinId=>{selectPin(pinId);return pullSelected()},restart:restartRun,complete:()=>{if(!run)return false;for(const pinId of run.level.solution){if(run.ended)break;const result=applyPin(run,pinId);if(!result.ok)return false}if(run.ended)finishResult(run.result.win,run.result.reason);renderBoard();return Boolean(run.result?.win)},setSave:value=>{save=normalizeSave(value);persist();renderMain();renderStages();renderWorkshop()},getSave:()=>JSON.parse(JSON.stringify(save))};
initLocale();applyLocale();setStageTab("brief");Promise.all([...document.images].filter(image=>!image.complete).map(image=>new Promise(resolve=>{image.addEventListener("load",resolve,{once:true});image.addEventListener("error",resolve,{once:true})}))).finally(()=>{els.loading.hidden=true;renderMain()});
})();
