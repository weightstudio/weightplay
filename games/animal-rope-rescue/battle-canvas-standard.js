// Animal Vine Rescue owns a dedicated portrait/short-landscape Kids Canvas in game.js.
// This local marker prevents the async public-guide loader from installing the shared
// portrait-only scaler while preserving the standard Canvas CSS contract.
