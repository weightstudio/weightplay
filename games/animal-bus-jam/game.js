(()=>{
"use strict";
const codes=["en","zh-Hant","zh-Hans","ja","ko","es","pt-BR","fr","de","it","ru","hi","ar"];
const root=document, levels=window.BUS_JAM_LEVELS.levels, dict=window.BUS_JAM_LOCALES;
let locale="en", screen="main", selected=0, levelIndex=0, queues=[], busCounts=[], history=[], moves=0;
const $=id=>root.getElementById(id), key="animalBusJamProgress";
let progress=JSON.parse(localStorage.getItem(key)||"[]");
const t=(k,vars={})=>{const d=dict[locale]||dict.en;let v=d[k]??dict.en[k]??k;if(Array.isArray(v))return v;return String(v).replace(/\{(\w+)\}/g,(_,n)=>vars[n]??"");};
function save(){localStorage.setItem(key,JSON.stringify(progress))}
function applyLocale(){document.documentElement.lang=locale;document.documentElement.dir=locale==="ar"?"rtl":"ltr";root.querySelectorAll("[data-t]").forEach(e=>e.textContent=t(e.dataset.t));root.querySelectorAll("[data-t-aria]").forEach(e=>e.setAttribute("aria-label",t(e.dataset.tAria)));root.querySelectorAll("[data-t-alt]").forEach(e=>e.alt=t(e.dataset.tAlt));$("locale").value=locale;renderStage()}
function show(name){["main","stage","battle"].forEach(id=>$(id).hidden=id!==name);screen=name}
function renderStage(){if(screen!=="stage")return;$("progress").textContent=t("progress",{done:progress.filter(Boolean).length});$("stageGrid").innerHTML=levels.map((l,i)=>{const locked=i>0&&!progress[i-1];return `<button class="stage-card ${i===selected?"selected":""}" data-level="${i}" ${locked?"disabled":""}><strong>${t("stop",{n:i+1})}</strong><span>${locked?"🔒 "+t("locked"):progress[i]?"✓ "+t("complete"):t("chapter",{n:Math.floor(i/10)+1})}</span></button>`}).join("");root.querySelectorAll("[data-level]").forEach(b=>b.onclick=()=>{selected=+b.dataset.level;renderStage();startLevel(selected)});$("enter").disabled=selected>0&&!progress[selected-1]}
function render(){const l=levels[levelIndex];$("chapter").textContent=t("chapter",{n:Math.floor(levelIndex/10)+1});$("stageName").textContent=t("stop",{n:levelIndex+1});$("remaining").textContent=t("remaining",{n:queues.flat().length});$("buses").innerHTML=Array.from({length:3},(_,c)=>`<div class="bus" style="--bus:${["#ef7770","#5b9fe3","#efb83f"][c]}"><strong>${t("colors")[c]}</strong><div class="seats">${Array.from({length:l.capacity},(_,i)=>`<i class="${i<busCounts[c]?"filled":""}"></i>`).join("")}</div><small>${t("busLabel",{color:t("colors")[c],filled:busCounts[c],capacity:l.capacity})}</small></div>`).join("");$("queues").innerHTML=queues.map((q,i)=>`<div class="queue" style="--person:${["#ef7770","#5b9fe3","#efb83f"][q[0]??i]}">${q.length?q.map((c,j)=>`<button class="${j===0?"front":""}" data-queue="${i}" style="--person:${["#ef7770","#5b9fe3","#efb83f"][c]}" aria-label="${t("personLabel",{color:t("colors")[c]})}">${t("colors")[c]}</button>`).join(""):"<small>—</small>"}</div>`).join("");root.querySelectorAll("[data-queue]").forEach(b=>b.onclick=()=>board(+b.dataset.queue));$("status").textContent=t("status");$("undo").disabled=!history.length}
function startLevel(i){levelIndex=i;const l=levels[i];queues=l.queues.map(q=>q.slice());busCounts=[0,0,0];history=[];moves=0;show("battle");render()}
function board(i){if(!queues[i].length)return;const c=queues[i][0],l=levels[levelIndex];if(busCounts[c]>=l.capacity){$("status").textContent=t("full");return}history.push({queues:queues.map(q=>q.slice()),busCounts:busCounts.slice(),moves});queues[i].shift();busCounts[c]++;moves++;render();$("status").textContent=t("boarded");if(!queues.flat().length){progress[levelIndex]=true;save();$("resultBody").textContent=t("resultBody",{n:levelIndex+1,moves});$("result").showModal()}}
function undo(){const s=history.pop();if(!s)return;queues=s.queues;busCounts=s.busCounts;moves=s.moves;render()}
function hint(){const i=queues.findIndex(q=>q.length&&busCounts[q[0]]<levels[levelIndex].capacity);if(i>=0){const b=root.querySelector(`[data-queue="${i}"]`);b?.focus();b?.classList.add("hint");setTimeout(()=>b?.classList.remove("hint"),700)}}
$("start").onclick=()=>{selected=0;show("stage");renderStage()};root.querySelectorAll("[data-back]").forEach(b=>b.onclick=()=>show(screen==="battle"?"stage":"main"));$("undo").onclick=undo;$("hint").onclick=hint;$("restart").onclick=()=>startLevel(levelIndex);$("retry").onclick=()=>{$("result").close();startLevel(levelIndex)};$("next").onclick=()=>{$("result").close();if(levelIndex<levels.length-1){selected=levelIndex+1;show("stage");renderStage()}else show("stage")};$("locale").innerHTML=codes.map(c=>`<option value="${c}">${dict[c]?.label||c}</option>`).join("");$("locale").onchange=e=>{locale=e.target.value;applyLocale()};applyLocale();show("main");
})();
