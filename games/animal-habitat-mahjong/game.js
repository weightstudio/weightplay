(() => {
  const LOGICAL_W = 390, LOGICAL_H = 788, RESERVE = 56, saveKey = "weightplay_habitat_mahjong_v1";
  const $ = (id) => document.getElementById(id);
  const nodes = Object.fromEntries(["mainScreen","stageScreen","battleScreen","resultScreen","localeSelect","startBtn","stageRail","stagePreview","stageName","stageDescription","stageProgress","board","movesValue","pairsValue","battleStageName","objectiveText","feedback","hintBtn","undoBtn","shuffleBtn","retryBtn","nextBtn","stagesBtn","resultTitle","resultStats","resultReport","progressSummary","stageSlot","battleSlot","resultSlot"].map((id) => [id, $(id)]));
  const copy = {
    en: { language:"Language", internalPreview:"Internal preview", title:"Animal Habitat Mahjong", mainHint:"Match two free tiles to open every animal habitat.", startGame:"Start Game", howTitle:"How to play", guide:"A tile is free when nothing covers it and at least one side is open. Match two identical free tiles to clear the board.", parentNote:"A calm local puzzle for focus, logic, and problem solving. Progress stays on this device.", progress:"Unlocked {count}/3 habitats · Best {best} pairs", stageTitle:"Choose a habitat", railHint:"Drag to browse habitats. Tap an unlocked card to begin.", locked:"Locked", moves:"Moves", pairs:"Pairs", objective:"Clear every free pair", hint:"Hint", undo:"Undo", shuffle:"Shuffle", choosePair:"Choose two matching free tiles.", blocked:"That tile is covered or blocked on both sides.", noMatch:"Find a matching free tile.", matched:"Great match!", noUndo:"Nothing to undo yet.", hintText:"A free matching pair is glowing.", noMoves:"No free pair. Use Shuffle to refresh the board.", habitatCleared:"Habitat cleared", skillReport:"Skill Report", result:"You cleared {pairs} pairs in {moves} moves. Great planning and focus.", retry:"Retry", nextStage:"Next Stage", stages:"Stages", tile:"Tile {number}", back:"Back", backToLobby:"Back to lobby", stageScreen:"Stage selection", stageRail:"Habitat stages", battleScreen:"Mahjong play", resultScreen:"Result", forest:"Forest Canopy", safari:"Safari Trail", ocean:"Coral Shelf", forestDesc:"Open leafy paths and learn the free-tile rule.", safariDesc:"Plan around a raised center and wide open sides.", oceanDesc:"Use every match to uncover the calm coral shelf." },
    "zh-Hant": { language:"\u8a9e\u8a00", internalPreview:"\u5167\u90e8\u9810\u89bd", title:"\u52d5\u7269\u68f2\u5730\u9ebb\u5c07", mainHint:"\u914d\u5c0d\u5169\u5f35\u53ef\u7528\u724c\uff0c\u958b\u555f\u6bcf\u500b\u52d5\u7269\u68f2\u5730\u3002", startGame:"\u958b\u59cb\u904a\u6232", howTitle:"\u73a9\u6cd5", guide:"\u724c\u9762\u4e0a\u6c92\u6709\u88ab\u8986\u84cb\uff0c\u4e14\u5de6\u53f3\u81f3\u5c11\u4e00\u908a\u958b\u653e\u6642\uff0c\u5c31\u662f\u53ef\u7528\u724c\u3002\u914d\u5c0d\u5169\u5f35\u76f8\u540c\u53ef\u7528\u724c\u4f86\u6e05\u7a7a\u724c\u5c40\u3002", parentNote:"\u9019\u662f\u7df4\u7fd2\u5c08\u6ce8\u3001\u908f\u8f2f\u8207\u89e3\u6c7a\u554f\u984c\u7684\u5be7\u975c\u672c\u6a5f\u904a\u6232\u3002\u9032\u5ea6\u53ea\u5132\u5b58\u65bc\u6b64\u88dd\u7f6e\u3002", progress:"\u5df2\u89e3\u9396 {count}/3 \u500b\u68f2\u5730 \u00b7 \u6700\u4f73 {best} \u5c0d", stageTitle:"\u9078\u64c7\u68f2\u5730", railHint:"\u5de6\u53f3\u62d6\u66f3\u700f\u89bd\u68f2\u5730\uff0c\u9ede\u64ca\u5df2\u89e3\u9396\u5361\u724c\u958b\u59cb\u3002", locked:"\u672a\u89e3\u9396", moves:"\u6b65\u6578", pairs:"\u914d\u5c0d", objective:"\u6e05\u9664\u6240\u6709\u53ef\u7528\u724c", hint:"\u63d0\u793a", undo:"\u5fa9\u539f", shuffle:"\u6d17\u724c", choosePair:"\u9078\u64c7\u5169\u5f35\u76f8\u540c\u7684\u53ef\u7528\u724c\u3002", blocked:"\u9019\u5f35\u724c\u88ab\u8986\u84cb\u6216\u88ab\u5169\u5074\u5361\u4f4f\u3002", noMatch:"\u8acb\u627e\u4e00\u5f35\u76f8\u540c\u7684\u53ef\u7528\u724c\u3002", matched:"\u914d\u5c0d\u6210\u529f\uff01", noUndo:"\u9084\u6c92\u6709\u53ef\u5fa9\u539f\u7684\u914d\u5c0d\u3002", hintText:"\u4e00\u5c0d\u53ef\u7528\u724c\u6b63\u5728\u767c\u4eae\u3002", noMoves:"\u6c92\u6709\u53ef\u914d\u5c0d\u724c\uff0c\u4f7f\u7528\u6d17\u724c\u91cd\u65b0\u6574\u7406\u3002", habitatCleared:"\u68f2\u5730\u5df2\u6e05\u7a7a", skillReport:"\u80fd\u529b\u5831\u544a", result:"\u4f60\u7528 {moves} \u6b65\u6e05\u9664 {pairs} \u5c0d\u724c\uff0c\u5f88\u597d\u5730\u7df4\u7fd2\u4e86\u898f\u5283\u8207\u5c08\u6ce8\u3002", retry:"\u518d\u8a66\u4e00\u6b21", nextStage:"\u4e0b\u4e00\u95dc", stages:"\u95dc\u5361", tile:"\u724c {number}", back:"\u8fd4\u56de", backToLobby:"\u8fd4\u56de\u5927\u5ef3", stageScreen:"\u95dc\u5361\u9078\u64c7", stageRail:"\u68f2\u5730\u95dc\u5361", battleScreen:"\u9ebb\u5c07\u904a\u73a9", resultScreen:"\u7d50\u679c", forest:"\u68ee\u6797\u6a39\u51a0", safari:"\u8349\u539f\u8db3\u8de1", ocean:"\u73ca\u745a\u68da", forestDesc:"\u958b\u555f\u6a39\u8449\u8def\u5f91\uff0c\u5b78\u7fd2\u53ef\u7528\u724c\u898f\u5247\u3002", safariDesc:"\u570d\u7e5e\u62ac\u9ad8\u7684\u4e2d\u5fc3\u724c\u89c0\u5bdf\u5bec\u655e\u958b\u53e3\u3002", oceanDesc:"\u7528\u6bcf\u4e00\u6b21\u914d\u5c0d\u6253\u958b\u5be7\u975c\u73ca\u745a\u68da\u3002" }
  };
  Object.assign(copy.en, { boardName:"{habitat} Board {number}", arctic:"Arctic Glow", arcticDesc:"Use the top layers to reveal a clear snowy route." });
  Object.assign(copy["zh-Hant"], { boardName:"{habitat} \u7b2c {number} \u95dc", arctic:"\u6975\u5730\u6975\u5149", arcticDesc:"\u5148\u6253\u958b\u4e0a\u5c64\u724c\uff0c\u627e\u51fa\u6e05\u6670\u7684\u96ea\u5730\u8def\u7dda\u3002" });
  const layouts = {
    grid: [[[0,0,0],[3,0,0]],[[1,0,0],[2,0,0]],[[0,1,0],[3,1,0]],[[1,1,0],[2,1,0]],[[0,2,0],[3,2,0]],[[1,2,0],[2,2,0]],[[0,3,0],[3,3,0]],[[1,3,0],[2,3,0]]],
    diamond: [[[1,0,0],[2,0,0]],[[0,1,0],[3,1,0]],[[1,1,0],[2,1,0]],[[0,2,0],[3,2,0]],[[1,2,0],[2,2,0]],[[0,3,0],[3,3,0]],[[1,3,0],[2,3,0]],[[1,4,0],[2,4,0]]],
    terrace: [[[0,0,0],[3,0,0]],[[1,0,0],[2,0,0]],[[1,1,0],[2,1,0]],[[0,2,0],[3,2,0]],[[1,2,0],[2,2,0]],[[1,3,0],[2,3,0]],[[0,4,0],[3,4,0]],[[1,4,0],[2,4,0]]],
    stack: [[[1,1,1],[2,1,1]],[[1,2,1],[2,2,1]],[[0,0,0],[3,0,0]],[[1,0,0],[2,0,0]],[[0,1,0],[3,1,0]],[[1,1,0],[2,1,0]],[[0,2,0],[3,2,0]],[[1,2,0],[2,2,0]],[[0,3,0],[3,3,0]],[[1,3,0],[2,3,0]]],
    crown: [[[1,0,1],[2,0,1]],[[1,1,1],[2,1,1]],[[0,0,0],[3,0,0]],[[1,0,0],[2,0,0]],[[0,1,0],[3,1,0]],[[1,1,0],[2,1,0]],[[0,2,0],[3,2,0]],[[1,2,0],[2,2,0]],[[0,3,0],[3,3,0]],[[1,3,0],[2,3,0]],[[1,4,0],[2,4,0]]]
  };
  const habitats = [
    ["forest","grid"],["forest","diamond"],["forest","terrace"],["forest","stack"],
    ["safari","grid"],["safari","diamond"],["safari","terrace"],["safari","crown"],
    ["ocean","grid"],["ocean","diamond"],["ocean","stack"],["ocean","crown"],
    ["arctic","terrace"],["arctic","stack"],["arctic","grid", true]
  ].map(([bg, template, noMove = false]) => ({ id:bg, bg, title:bg, desc:`${bg}Desc`, template, noMove }));
  let locale = localStorage.getItem("weightPlayLocale") || "en", save = loadSave(), stageIndex = 0, selected = null, state = null, drag = null;
  const t = (key, values = {}) => Object.entries(values).reduce((out, [name, value]) => out.replaceAll(`{${name}}`, value), (copy[locale] || copy.en)[key] || copy.en[key] || key);
  function loadSave() { try { return { unlocked:1, bestPairs:0, playCount:0, ...JSON.parse(localStorage.getItem(saveKey) || "{}") }; } catch { return { unlocked:1, bestPairs:0, playCount:0 }; } }
  const persist = () => localStorage.setItem(saveKey, JSON.stringify(save));
  function applyLocale() { document.documentElement.lang = locale; document.title = `${t("title")} - WeightPlay`; document.querySelectorAll("[data-ui]").forEach((el) => { el.textContent = t(el.dataset.ui); }); document.querySelectorAll("[data-aria]").forEach((el) => { el.setAttribute("aria-label", t(el.dataset.aria)); }); nodes.localeSelect.value = locale; renderMain(); renderStage(); if (state) renderBattle(); }
  function setScreen(name) { ["main","stage","battle","result"].forEach((key) => nodes[`${key}Screen`].classList.toggle("hidden", key !== name)); document.body.classList.toggle("playing", name !== "main"); if (name !== "main") fitCanvases(); }
  function fitCanvases() { const h = Math.max(1, innerHeight - RESERVE), scale = Math.min(innerWidth / LOGICAL_W, h / LOGICAL_H); document.documentElement.style.setProperty("--scale", scale); document.documentElement.style.setProperty("--slot-w", `${LOGICAL_W * scale}px`); document.documentElement.style.setProperty("--slot-h", `${LOGICAL_H * scale}px`); }
  const stageLabel = (habitat, index = stageIndex) => t("boardName", { habitat:t(habitat.title), number:index + 1 });
  function renderMain() { nodes.progressSummary.textContent = t("progress", { count: Math.min(habitats.length, save.unlocked), best: save.bestPairs }).replace("/3", `/${habitats.length}`); }
  function renderStage() { const habitat = habitats[stageIndex]; nodes.stagePreview.src = `../../assets/animal-habitat-mahjong-album-${habitat.id}.webp`; nodes.stageName.textContent = stageLabel(habitat); nodes.stageDescription.textContent = t(habitat.desc); nodes.stageProgress.textContent = `${Math.min(save.unlocked, habitats.length)}/${habitats.length}`; nodes.stageRail.replaceChildren(...habitats.map((item, index) => { const button = document.createElement("button"); const unlocked = index < save.unlocked; button.className = `stage-card${index === stageIndex ? " selected" : ""}${unlocked ? "" : " locked"}`; button.dataset.index = index; button.dataset.stage = index + 1; button.innerHTML = `<b>${stageLabel(item, index)}</b><span>${t(item.desc)}</span><small>${unlocked ? t("startGame") : t("locked")}</small>`; return button; })); }
  function makeTiles(habitat) { let id = 0; return layouts[habitat.template].flatMap((pair, pairIndex) => pair.map(([x,y,layer], memberIndex) => ({ id:id++, value:habitat.noMove ? pairIndex - (pairIndex % 2) + memberIndex : (stageIndex * 3 + pairIndex) % 24, x, y, layer, removed:false }))); }
  function startStage(index = stageIndex) { stageIndex = index; const tiles = makeTiles(habitats[index]); state = { tiles, totalPairs:tiles.length / 2, moves:0, removedPairs:0, history:[], hinted:[], selected:null }; save.playCount += 1; persist(); setScreen("battle"); renderBattle(availablePairs().length ? "" : t("noMoves")); }
  function isFree(tile) { if (tile.removed) return false; const active = state.tiles.filter((item) => !item.removed); const covered = active.some((item) => item.layer > tile.layer && item.x === tile.x && item.y === tile.y); const left = active.some((item) => item.layer === tile.layer && item.y === tile.y && item.x === tile.x - 1); const right = active.some((item) => item.layer === tile.layer && item.y === tile.y && item.x === tile.x + 1); return !covered && (!left || !right); }
  function availablePairs() { const free = state.tiles.filter(isFree); const groups = new Map(); free.forEach((tile) => groups.set(tile.value, [...(groups.get(tile.value) || []), tile])); return [...groups.values()].filter((items) => items.length > 1); }
  function renderBattle(message = "") { const habitat = habitats[stageIndex]; nodes.battleStageName.textContent = stageLabel(habitat); nodes.objectiveText.textContent = t("objective"); nodes.movesValue.textContent = state.moves; nodes.pairsValue.textContent = `${state.removedPairs}/${state.totalPairs}`; nodes.feedback.textContent = message || t("choosePair"); nodes.shuffleBtn.disabled = availablePairs().length > 0; nodes.board.style.backgroundImage = `linear-gradient(#06223799,#06223799),url(../../assets/animal-habitat-mahjong-bg-${habitat.bg}.webp)`; nodes.board.replaceChildren(...state.tiles.filter((tile) => !tile.removed).map((tile) => { const button = document.createElement("button"); button.className = `tile${isFree(tile) ? " free" : " blocked"}${state.selected?.id === tile.id ? " selected" : ""}${state.hinted.includes(tile.id) ? " hinted" : ""}`; button.style.left = `${14 + tile.x * 82 + tile.layer * 7}px`; button.style.top = `${52 + tile.y * 84 - tile.layer * 7}px`; button.style.zIndex = String(2 + tile.layer); button.dataset.tile = tile.id; button.setAttribute("aria-label", t("tile", { number: tile.value + 1 })); const img = document.createElement("img"); img.src = `../../assets/animal-habitat-mahjong-tile-${String(tile.value).padStart(2,"0")}.webp`; img.alt = ""; button.append(img); return button; })); }
  function chooseTile(id) { const tile = state.tiles.find((item) => item.id === id); if (!tile || !isFree(tile)) return renderBattle(t("blocked")); if (!state.selected) { state.selected = tile; return renderBattle(t("choosePair")); } if (state.selected.id === tile.id) { state.selected = null; return renderBattle(); } if (state.selected.value !== tile.value) { state.selected = tile; return renderBattle(t("noMatch")); } const first = state.selected; first.removed = tile.removed = true; state.history.push([first.id, tile.id]); state.selected = null; state.moves += 1; state.removedPairs += 1; state.hinted = []; save.bestPairs = Math.max(save.bestPairs, state.removedPairs); persist(); if (state.removedPairs === state.totalPairs) return finishStage(); renderBattle(availablePairs().length ? t("matched") : t("noMoves")); }
  function hint() { const pair = availablePairs()[0]; if (!pair) return renderBattle(t("noMoves")); state.hinted = pair.map((tile) => tile.id); renderBattle(t("hintText")); }
  function undo() { const pair = state.history.pop(); if (!pair) return renderBattle(t("noUndo")); pair.forEach((id) => { state.tiles.find((tile) => tile.id === id).removed = false; }); state.removedPairs -= 1; state.moves = Math.max(0, state.moves - 1); state.selected = null; renderBattle(); }
  function shuffle() { const visible = state.tiles.filter((tile) => !tile.removed); const values = visible.map((tile) => tile.value).sort(() => Math.random() - .5); visible.forEach((tile, index) => { tile.value = values[index]; }); const free = visible.filter(isFree); if (!availablePairs().length && free.length > 1) { const first = free[0], second = free[1], firstMate = visible.find((tile) => tile.id !== first.id && tile.value === first.value); if (firstMate) { const secondValue = second.value; second.value = first.value; firstMate.value = secondValue; } } state.selected = null; state.moves += 1; renderBattle(t("matched")); }
  function finishStage() { save.unlocked = Math.max(save.unlocked, Math.min(habitats.length, stageIndex + 2)); persist(); nodes.resultTitle.textContent = stageLabel(habitats[stageIndex]); nodes.resultStats.textContent = `${state.removedPairs} ${t("pairs")} · ${state.moves} ${t("moves")}`; nodes.resultReport.textContent = t("result", { pairs: state.removedPairs, moves: state.moves }); nodes.nextBtn.classList.toggle("hidden", stageIndex >= habitats.length - 1); setScreen("result"); }
  nodes.localeSelect.addEventListener("change", (event) => { locale = event.target.value; localStorage.setItem("weightPlayLocale", locale); applyLocale(); });
  nodes.startBtn.addEventListener("click", () => { stageIndex = Math.max(0, Math.min(habitats.length, save.unlocked) - 1); setScreen("stage"); renderStage(); }); document.querySelectorAll("[data-back]").forEach((button) => button.addEventListener("click", () => { if (button.dataset.back === "stage") renderStage(); setScreen(button.dataset.back); }));
  nodes.stageRail.addEventListener("pointerdown", (event) => { if (nodes.stageRail.dataset.wpStageRail === "true") return; drag = { x:event.clientX, start:stageIndex, moved:false, cardIndex:Number(event.target.closest(".stage-card")?.dataset.index) }; nodes.stageRail.setPointerCapture?.(event.pointerId); }); nodes.stageRail.addEventListener("pointermove", (event) => { if (!drag) return; const next = Math.max(0, Math.min(habitats.length - 1, drag.start + Math.round((drag.x - event.clientX) / 120))); if (next !== stageIndex) { stageIndex = next; renderStage(); } drag.moved ||= Math.abs(event.clientX - drag.x) > 8; }); nodes.stageRail.addEventListener("pointerup", () => { if (!drag) return; const { moved, cardIndex } = drag; drag = null; if (!moved && Number.isInteger(cardIndex) && cardIndex < save.unlocked) startStage(cardIndex); });
  nodes.stageRail.addEventListener("click", (event) => { const card = event.target.closest(".stage-card:not(.locked)"); if (card) startStage(Number(card.dataset.index)); });
  nodes.board.addEventListener("click", (event) => { const tile = event.target.closest(".tile"); if (tile) chooseTile(Number(tile.dataset.tile)); }); nodes.hintBtn.addEventListener("click", hint); nodes.undoBtn.addEventListener("click", undo); nodes.shuffleBtn.addEventListener("click", shuffle); nodes.retryBtn.addEventListener("click", () => startStage(stageIndex)); nodes.nextBtn.addEventListener("click", () => startStage(Math.min(habitats.length - 1, stageIndex + 1))); nodes.stagesBtn.addEventListener("click", () => { stageIndex = Math.max(0, Math.min(habitats.length, save.unlocked) - 1); setScreen("stage"); renderStage(); });
  window.addEventListener("resize", fitCanvases, { passive:true }); window.visualViewport?.addEventListener("resize", fitCanvases, { passive:true }); applyLocale(); renderMain(); fitCanvases();
})();
