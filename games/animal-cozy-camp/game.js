(function () {
  "use strict";
  const locales = window.ANIMAL_COZY_CAMP_LOCALES || {};
  const localeKeys = window.ANIMAL_COZY_CAMP_LOCALE_KEYS || ["en"];
  const rounds = [
    { title: "roundOne", clues: ["r1c1", "r1c2"], layouts: [["orla", "moss", "taro", "pip"], ["orla", "taro", "moss", "pip"], ["moss", "orla", "pip", "taro"]], solution: 0 },
    { title: "roundTwo", clues: ["r2c1", "r2c2"], layouts: [["taro", "pip", "orla", "moss"], ["taro", "orla", "pip", "moss"], ["pip", "taro", "moss", "orla"]], solution: 0 },
    { title: "roundThree", clues: ["r3c1", "r3c2"], layouts: [["moss", "taro", "pip", "orla"], ["moss", "pip", "taro", "orla"], ["orla", "moss", "taro", "pip"]], solution: 0 }
  ];
  const state = { locale: "en", sound: true, round: 0, selected: null, checks: 0, totalChecks: 0, view: "main" };
  const $ = (id) => document.getElementById(id);
  const t = (key, vars = {}) => { const table = locales[state.locale] || locales.en || {}; let value = String(table[key] || locales.en?.[key] || key); Object.entries(vars).forEach(([name, replacement]) => { value = value.replaceAll(`{${name}}`, String(replacement)); }); return value; };
  const show = (view) => { state.view = view; document.querySelectorAll("[data-view]").forEach((node) => { node.hidden = node.dataset.view !== view; }); document.body.dataset.screen = view; window.scrollTo(0, 0); };
  const storageKey = "weightplay-animal-cozy-camp-best-v1";
  const readBest = () => { try { const value = Number(localStorage.getItem(storageKey)); return Number.isFinite(value) && value > 0 ? value : null; } catch (_) { return null; } };
  const saveBest = () => { try { const old = readBest(); if (!old || state.totalChecks < old) localStorage.setItem(storageKey, String(state.totalChecks)); } catch (_) {} };
  const track = (event, detail = {}) => { window.dispatchEvent(new CustomEvent("weightplay:analytics", { detail: { game: "animal-cozy-camp", event, ...detail } })); };
  const tone = (frequency) => { if (!state.sound) return; try { const Audio = window.AudioContext || window.webkitAudioContext; if (!Audio) return; const context = new Audio(); const oscillator = context.createOscillator(); const gain = context.createGain(); oscillator.frequency.value = frequency; gain.gain.setValueAtTime(0.0001, context.currentTime); gain.gain.exponentialRampToValueAtTime(0.025, context.currentTime + 0.01); gain.gain.exponentialRampToValueAtTime(0.0001, context.currentTime + 0.12); oscillator.connect(gain).connect(context.destination); oscillator.start(); oscillator.stop(context.currentTime + 0.13); oscillator.addEventListener("ended", () => context.close(), { once: true }); } catch (_) {} };
  const applyLocale = () => { const copy = locales[state.locale] || locales.en; document.documentElement.lang = state.locale === "zh-Hant" ? "zh-TW" : state.locale; document.documentElement.dir = copy.direction || "ltr"; document.querySelectorAll("[data-copy]").forEach((node) => { node.textContent = t(node.dataset.copy); }); $("sound-toggle").textContent = state.sound ? t("soundOn") : t("soundOff"); $("sound-toggle").setAttribute("aria-pressed", String(state.sound)); $("locale-select").value = state.locale; if (state.view === "battle") renderBattle(); if (state.view === "result") renderResult(); };
  const populateLocales = () => { const select = $("locale-select"); select.replaceChildren(...localeKeys.map((key) => { const option = document.createElement("option"); option.value = key; option.textContent = locales.en.languageNames[key] || key; return option; })); select.addEventListener("change", () => { state.locale = select.value; try { localStorage.setItem("weightplay-animal-cozy-camp-locale", state.locale); } catch (_) {} applyLocale(); }); };
  const openDialog = (id) => { const dialog = $(id); if (typeof dialog.showModal === "function") dialog.showModal(); else dialog.setAttribute("open", ""); };
  const closeDialog = (id) => { const dialog = $(id); if (typeof dialog.close === "function") dialog.close(); else dialog.removeAttribute("open"); };
  const start = () => { state.round = 0; state.selected = null; state.checks = 0; state.totalChecks = 0; show("battle"); renderBattle(); track("camp_start"); };
  const renderBattle = () => { const round = rounds[state.round]; $("round-title").textContent = t(round.title); $("round-count").textContent = `${state.round + 1}/${rounds.length}`; const clues = $("clue-list"); clues.replaceChildren(...round.clues.map((key) => { const item = document.createElement("li"); item.textContent = t(key); return item; })); const choices = $("layout-choices"); choices.replaceChildren(...round.layouts.map((layout, index) => { const button = document.createElement("button"); button.type = "button"; button.className = "layout-card"; button.setAttribute("role", "listitem"); button.setAttribute("aria-pressed", String(state.selected === index)); button.classList.toggle("is-selected", state.selected === index); const title = document.createElement("strong"); title.textContent = t(index === 0 ? "layoutA" : index === 1 ? "layoutB" : "layoutC"); const order = document.createElement("span"); order.className = "seat-order"; layout.forEach((name) => { const seat = document.createElement("span"); seat.className = "seat-chip"; seat.textContent = t(name); order.append(seat); }); button.append(title, order); button.addEventListener("click", () => { state.selected = index; $("selection-note").textContent = t("selected", { n: index + 1 }); $("battle-status").textContent = t("choose"); $("battle-status").dataset.kind = ""; $("check-button").disabled = false; renderBattle(); track("layout_select", { round: state.round + 1, layout: index + 1 }); }); return button; })); $("check-button").disabled = state.selected === null; };
  const renderResult = () => { $("result-text").textContent = t("resultText", { checks: state.totalChecks }); $("result-checks").textContent = String(state.totalChecks); $("result-best").textContent = String(readBest() || t("noBest")); $("result-status").textContent = t("correct"); };
  const reset = () => { state.selected = null; $("selection-note").textContent = ""; $("battle-status").textContent = t("choose"); $("battle-status").dataset.kind = ""; renderBattle(); track("layout_reset", { round: state.round + 1 }); };
  const check = () => { if (state.selected === null) return; state.checks += 1; state.totalChecks += 1; if (state.selected !== rounds[state.round].solution) { $("battle-status").textContent = t("wrong"); $("battle-status").dataset.kind = "wrong"; tone(190); track("layout_check", { round: state.round + 1, result: "wrong" }); return; } $("battle-status").textContent = t("correct"); $("battle-status").dataset.kind = "correct"; tone(680); track("layout_check", { round: state.round + 1, result: "correct" }); window.setTimeout(() => { if (state.round < rounds.length - 1) { state.round += 1; state.selected = null; $("selection-note").textContent = ""; renderBattle(); } else { saveBest(); renderResult(); show("result"); track("camp_complete"); } }, 220); };
  const goHome = () => { closeDialog("leave-dialog"); show("main"); applyLocale(); };
  const requestHome = () => openDialog("leave-dialog");
  state.locale = (() => { try { const saved = localStorage.getItem("weightplay-animal-cozy-camp-locale"); if (saved && locales[saved]) return saved; } catch (_) {} const query = new URLSearchParams(location.search).get("lang"); return locales[query] ? query : "en"; })();
  document.addEventListener("DOMContentLoaded", () => { populateLocales(); applyLocale(); $("start-button").addEventListener("click", start); $("guide-button").addEventListener("click", () => openDialog("guide-dialog")); $("guide-start").addEventListener("click", () => { closeDialog("guide-dialog"); start(); }); $("close-guide").addEventListener("click", () => closeDialog("guide-dialog")); $("main-settings").addEventListener("click", () => openDialog("settings-dialog")); $("battle-settings").addEventListener("click", () => openDialog("settings-dialog")); $("result-settings").addEventListener("click", () => openDialog("settings-dialog")); $("close-settings").addEventListener("click", () => closeDialog("settings-dialog")); $("sound-toggle").addEventListener("click", () => { state.sound = !state.sound; applyLocale(); }); $("battle-home").addEventListener("click", requestHome); $("cancel-leave").addEventListener("click", () => closeDialog("leave-dialog")); $("keep-playing").addEventListener("click", () => closeDialog("leave-dialog")); $("confirm-leave").addEventListener("click", goHome); $("reset-button").addEventListener("click", reset); $("check-button").addEventListener("click", check); $("replay-button").addEventListener("click", start); $("home-button").addEventListener("click", () => { show("main"); applyLocale(); }); window.setTimeout(() => { $("loading-view").hidden = true; show("main"); }, 80); });
  window.ANIMAL_COZY_CAMP_TEST = { rounds, start, reset, check, getState: () => ({ ...state }) };
})();
