(function(){
"use strict";
const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
const STORE="weightplay:animal-sketchwheel-rally:v1", locales=window.SKETCHWHEEL_LOCALES||{}, en=locales.en||{}, localeNames={en:"English",'zh-Hant':"繁體中文",'zh-Hans':"简体中文",ja:"日本語",ko:"한국어",es:"Español",'pt-BR':"Português (Brasil)",fr:"Français",de:"Deutsch",it:"Italiano",ru:"Русский"};
const memory={};
function load(){try{return JSON.parse(localStorage.getItem(STORE)||"null")||defaults()}catch(_){return memory.save||defaults()}}
function persist(){try{localStorage.setItem(STORE,JSON.stringify(save))}catch(_){memory.save=JSON.parse(JSON.stringify(save))}}
function defaults(){return{locale:"en",unlocked:1,cleared:[],bolts:0,upgrades:[0,0,0],lens:false,tutorial:false,best:{}}}
let save=load(), locale=locales[save.locale]?save.locale:"en", t=locales[locale]||locales.en, selected=0, centered=0, run=null, raf=0, last=0, paused=false, drawing=false, drawPoints=[], forgeFeedbackTimer=0;
const clamp=(n,a=0,b=1)=>Math.max(a,Math.min(b,n)), fmt=(s,v={})=>String(s).replace(/\{(\w+)\}/g,(_,k)=>v[k]??"");
function track(event,data={}){try{window.WonderAnalytics?.track?.(event,{game_id:"animal-sketchwheel-rally",...data})}catch(_){}}
function sound(name){try{window.WonderSound?.play?.(name)}catch(_){}}
const terrainSequence=[
 ["flat","ice"],["flat","stairs"],["flat","stairs","flat"],["ice","flat","stairs"],["stairs","flat","ice"],
 ["flat","stairs","stairs"],["stairs","ice","flat"],["stairs","wind","flat"],["stairs","mud","flat"],["ice","stairs","wind"],
 ["flat","mud","flat"],["mud","stairs","mud"],["mud","ice","flat"],["wind","mud","stairs"],["mud","wind","ice"],
 ["stairs","tunnel","flat"],["tunnel","mud","stairs"],["ice","tunnel","wind"],["stairs","tunnel","mud","flat"],["wind","tunnel","stairs","ice"],
 ["flat","gap","flat"],["stairs","gap","mud"],["wind","gap","tunnel"],["gap","ice","gap"],["mud","gap","wind","stairs"],
 ["stairs","mud","tunnel","gap"],["ice","wind","gap","mud"],["tunnel","stairs","gap","ice"],["mud","gap","tunnel","wind","stairs"],["ice","stairs","mud","tunnel","gap","wind"]
];
const levels=terrainSequence.map((terrain,i)=>({id:i+1,chapter:Math.floor(i/5),terrain,time:54+terrain.length*6,positions:terrain.map((_,j)=>150+j*(720/Math.max(1,terrain.length-1))),rival:18.5+i*.18}));
const presets={circle:{speed:.92,height:.56,grip:.38,width:.62,roundness:.92,kind:"circle"},tall:{speed:.54,height:.92,grip:.48,width:.38,roundness:.48,kind:"tall"},spike:{speed:.48,height:.63,grip:.95,width:.64,roundness:.28,kind:"spike"},wide:{speed:.64,height:.46,grip:.52,width:.94,roundness:.58,kind:"wide"}};
const required={flat:"circle",stairs:"tall",mud:"spike",tunnel:"wide",gap:"wide",ice:"circle",wind:"tall"};
function profilePass(type,p){const tol=(save.upgrades[1]||0)*.035;if(type==="flat")return p.roundness>=.70-tol;if(type==="stairs")return p.height>=.76-tol;if(type==="mud")return p.grip>=.72-tol;if(type==="tunnel")return p.height<=.54+tol&&p.width>=.68-tol;if(type==="gap")return p.width>=.80-tol;if(type==="ice")return p.roundness>=.70-tol;if(type==="wind")return p.width<=.48+tol;return true}
function measure(points,w=240,h=116){if(!points||points.length<4)return null;let xs=points.map(p=>p.x),ys=points.map(p=>p.y),minX=Math.min(...xs),maxX=Math.max(...xs),minY=Math.min(...ys),maxY=Math.max(...ys),bw=maxX-minX,bh=maxY-minY;if(bw<16||bh<12)return null;let cx=(minX+maxX)/2,cy=(minY+maxY)/2,r=points.map(p=>Math.hypot((p.x-cx)/(bw||1),(p.y-cy)/(bh||1))),avg=r.reduce((a,b)=>a+b,0)/r.length,variance=Math.sqrt(r.reduce((a,b)=>a+(b-avg)**2,0)/r.length);let corners=0;for(let i=2;i<points.length;i++){let a=points[i-2],b=points[i-1],c=points[i],u=Math.atan2(b.y-a.y,b.x-a.x),v=Math.atan2(c.y-b.y,c.x-b.x),d=Math.abs(Math.atan2(Math.sin(v-u),Math.cos(v-u)));if(d>.55)corners++}let width=clamp(bw/(w*.86)),height=clamp(bh/(h*.78)),roundness=clamp(1-variance*2.4-Math.abs(width-height)*.22),grip=clamp(.25+corners/10+(1-roundness)*.35);return{speed:clamp(.35+roundness*.62),height,grip,width,roundness,kind:"custom",points:points.map(p=>({x:p.x/w,y:p.y/h}))}}
function setLocale(code){locale=locales[code]?code:"en";t=locales[locale];save.locale=locale;persist();document.documentElement.lang=locale;document.title=t.title+" | Internal WeightPlay Trial";$$('[data-t]').forEach(el=>{const v=t[el.dataset.t];if(typeof v==="string")el.textContent=v});$$('[data-t-alt]').forEach(el=>el.alt=t[el.dataset.tAlt]||"");$$('[data-t-aria]').forEach(el=>el.setAttribute("aria-label",t[el.dataset.tAria]||""));buildGuide();renderDynamic()}
function initLocales(){const s=$("#localeSelect");s.innerHTML=Object.keys(localeNames).map(k=>`<option value="${k}">${localeNames[k]}</option>`).join("");s.value=locale;s.addEventListener("change",()=>setLocale(s.value))}
function show(name){document.body.dataset.screen=name;$("#mainGroup").hidden=name!=="main";$("#stageScreen").hidden=name!=="stage";$("#battleScreen").hidden=name!=="battle";if(name!=="battle")cancelAnimationFrame(raf);if(name==="stage"){selected=Math.max(0,Math.min(save.unlocked-1,29));setTimeout(()=>centerCard(selected,false),50)}}
function renderDynamic(){const done=save.cleared.length;$("#mainProgress").textContent=fmt(t.progress,{done});$("#stageSummary").textContent=fmt(t.progress,{done});renderStages();renderForge();if(run)renderBattleText()}
function stageArt(i){return `../../assets/animal-sketchwheel-rally/animal-sketchwheel-rally-${i%2?"track-bg":"cover"}.webp`}
function renderStages(){const rail=$("#stageRail");rail.innerHTML=levels.map((l,i)=>{const lock=i>=save.unlocked,rule=t.chapterRules[l.chapter]||locales.en.chapterRules[l.chapter];return `<button class="stage-card${lock?' locked':''}" data-stage-index="${i}" data-index="${i}" ${lock?'data-locked aria-disabled="true"':''} type="button" aria-label="${fmt(t.race,{n:i+1})}"><img src="${stageArt(i)}" alt=""><span><small>${fmt(t.chapter,{n:l.chapter+1})} · ${lock?t.locked:rule}</small><strong>${fmt(t.race,{n:i+1})}</strong><small>${(t.chapterNames||locales.en.chapterNames)[l.chapter]}</small></span></button>`}).join("");$$('.stage-card').forEach(card=>card.addEventListener("click",()=>{const i=+card.dataset.index;if(i>=save.unlocked)return;selected=i;centerCard(i,true);setTimeout(()=>startRace(i),180)}));centerCard(Math.min(selected,save.unlocked-1),false)}
function centerCard(i,smooth=true){const cards=$$('.stage-card');if(!cards.length)return;centered=i;cards.forEach((c,n)=>{const active=n===i;c.classList.toggle("is-centered",active);if(active)c.setAttribute("aria-current","true");else c.removeAttribute("aria-current")});const c=cards[i];if(c)c.scrollIntoView({behavior:smooth?"smooth":"auto",block:"nearest",inline:"center"});selected=i;const l=levels[i];$("#selectedNumber").textContent=i+1;$("#selectedName").textContent=(t.chapterNames||locales.en.chapterNames)[l.chapter];$("#selectedRule").textContent=(t.chapterRules||locales.en.chapterRules)[l.chapter];$("#enterRace").disabled=i>=save.unlocked}
function nearestCard(){const rail=$("#stageRail"),mid=rail.getBoundingClientRect().left+rail.clientWidth/2;let best=0,d=Infinity;$$('.stage-card').forEach((c,i)=>{const r=c.getBoundingClientRect(),x=Math.abs(r.left+r.width/2-mid);if(x<d){d=x;best=i}});if(best!==centered)centerCard(best,false)}
function renderForge(){$("#boltCount").textContent=fmt(t.bolts,{n:save.bolts});$("#upgradeList").innerHTML=(t.upgradeNames||en.upgradeNames).map((name,i)=>{const lv=save.upgrades[i],cost=12+lv*12;return `<article class="upgrade-card"><strong>${name}</strong><p>${(t.upgradeDescs||en.upgradeDescs)[i]}</p><small>${fmt(t.level,{n:lv})}</small><button data-upgrade="${i}" ${lv>=3?'disabled':''}>${lv>=3?t.maxed:fmt(t.upgradeBuy,{n:cost})}</button></article>`}).join("");$$('[data-upgrade]').forEach(b=>b.onclick=()=>buyUpgrade(+b.dataset.upgrade));$("#lensBuy").textContent=save.lens?t.lensOwned:`${t.lens} · ${t.lensBuy}`;$("#lensBuy").disabled=save.lens}
function buyUpgrade(i){let cost=12+save.upgrades[i]*12;if(save.bolts<cost)return feedbackStage(fmt(t.needBolts,{cost,n:save.bolts}));save.bolts-=cost;save.upgrades[i]++;persist();renderForge();feedbackStage(fmt(t.upgradeComplete,{name:t.upgradeNames[i],level:save.upgrades[i]}))}
function buyLens(){if(save.lens)return;let balance=0,ok=false;try{balance=Number(window.WeightPlayWallet?.read?.().diamonds||0);ok=!!window.WeightPlayWallet?.spendDiamonds?.(15)}catch(_){ok=false}if(!ok)return feedbackStage(fmt(t.needDiamondsDetail,{n:balance}));save.lens=true;persist();renderForge();feedbackStage(t.lensComplete)}
function feedbackStage(s){clearTimeout(forgeFeedbackTimer);$("#forgeFeedback").textContent=s;forgeFeedbackTimer=setTimeout(()=>$("#forgeFeedback").textContent="",3000)}
function startRace(i){if(i>=save.unlocked)return;selected=i;const l=levels[i];run={index:i,player:0,rival:0,time:l.time,redraws:0,profile:{...presets.circle},terrainIndex:0,passed:[],ended:false,elapsed:0,speed:0,stalled:false,firstProfile:true};track("game_start",{race:i+1});sound("click");paused=true;show("battle");renderBattleText();drawPad(run.profile);$("#feedback").textContent=t.drawFirst;$("#feedback").classList.add("warn");if(!save.tutorial){openModal("tutorialModal")}else{paused=false;resumeLoop()}resizeCanvas();drawRace()}
function renderBattleText(){if(!run)return;const l=levels[run.index],nextType=l.terrain[Math.min(run.terrainIndex,l.terrain.length-1)];$("#battleChapter").textContent=fmt(t.chapter,{n:l.chapter+1});$("#battleRace").textContent=fmt(t.race,{n:run.index+1});$("#objective").textContent=t.objective;$("#nextTerrain").textContent=fmt(t.next,{terrain:t.terrainNames[nextType]});$("#profileStats").textContent=fmt(t.profileStats,{speed:Math.round(run.profile.speed*100),height:Math.round(run.profile.height*100),grip:Math.round(run.profile.grip*100),width:Math.round(run.profile.width*100)});$("#playerProgress").textContent=Math.round(run.player/10)+"%";$("#rivalProgress").textContent=Math.round(run.rival/10)+"%";$("#timeValue").textContent=Math.max(0,Math.ceil(run.time));if(save.lens)$("#nextTerrain").textContent+=` · ${t[required[nextType]]||required[nextType]}`}
function commitProfile(p){if(!run||run.ended||!p)return;if(!run.firstProfile)run.redraws++;run.firstProfile=false;run.profile={...p};run.stalled=false;track("wheel_profile_commit",{race:run.index+1,profile:p.kind||"custom"});sound("click");$("#feedback").textContent=t.profileReady;$("#feedback").classList.remove("warn");renderBattleText();drawPad(p)}
function simulate(dt){if(!run||run.ended||paused)return;if(run.firstProfile){run.speed=0;$("#feedback").textContent=t.drawFirst;$("#feedback").classList.add("warn");renderBattleText();return}const l=levels[run.index];run.elapsed+=dt;run.time-=dt;let type=l.terrain[run.terrainIndex],pos=l.positions[run.terrainIndex],near=type&&run.player>=pos-34&&run.player<=pos+54,pass=!type||profilePass(type,run.profile);let speed=15+run.profile.speed*17+(save.upgrades[2]||0)*1.4;if(near&&!pass){speed*=.08;run.stalled=true;$("#feedback").textContent=fmt(t.stalled,{terrain:t.terrainNames[type]});$("#feedback").classList.add("warn")}else if(near&&pass){speed*=1.12;if(run.stalled){run.stalled=false;$("#feedback").classList.remove("warn")}}run.speed=speed;run.player+=speed*dt;run.rival+=l.rival*dt;if(type&&run.player>pos+54){run.passed.push(type);run.terrainIndex++;$("#feedback").textContent=fmt(t.good,{terrain:t.terrainNames[type]});$("#feedback").classList.remove("warn")}if(run.player>=1000)return finish(true);if(run.rival>=1000||run.time<=0)return finish(false);renderBattleText()}
function finish(win){if(!run||run.ended)return;run.ended=true;paused=true;cancelAnimationFrame(raf);const l=levels[run.index],lead=win?Math.max(0,Math.round((run.player-run.rival)/l.rival)):0;$("#resultKicker").textContent=win?t.winKicker:t.failKicker;$("#resultTitle").textContent=win?t.winTitle:t.failTitle;$("#resultText").textContent=win?t.winText:t.failText;$("#resultRedraws").textContent=run.redraws;$("#resultLead").textContent=fmt(t.leadTime,{n:lead});const reward=win?10+l.terrain.length*3:2;$("#resultReward").textContent=fmt(t.rewardBolts,{n:reward});if(win){save.bolts+=reward;if(!save.cleared.includes(run.index))save.cleared.push(run.index);save.unlocked=Math.max(save.unlocked,Math.min(30,run.index+2));save.best[run.index]=Math.min(save.best[run.index]??999,run.elapsed);persist()}$("#nextRaceButton").hidden=!win||run.index>=29;openModal("resultModal")}
function openModal(id){paused=true;$("#"+id).hidden=false}
function closeModal(id,resume=true){$("#"+id).hidden=true;if(resume&&run&&!run.ended){paused=false;resumeLoop()}}
function resumeLoop(){cancelAnimationFrame(raf);last=performance.now();raf=requestAnimationFrame(loop)}
function loop(ts){let dt=Math.min(.04,(ts-last)/1000||0);last=ts;simulate(dt);drawRace();if(run&&!run.ended&&!paused)raf=requestAnimationFrame(loop)}
function resizeCanvas(){const c=$("#raceCanvas"),r=c.getBoundingClientRect(),d=Math.min(2,devicePixelRatio||1);c.width=Math.max(1,Math.round(r.width*d));c.height=Math.max(1,Math.round(r.height*d));c.dataset.dpr=d}
const bg=new Image(),vehicles=new Image(),obstacles=new Image();bg.src="../../assets/animal-sketchwheel-rally/animal-sketchwheel-rally-track-bg.webp";vehicles.src="../../assets/animal-sketchwheel-rally/animal-sketchwheel-rally-vehicle-atlas.png";obstacles.src="../../assets/animal-sketchwheel-rally/animal-sketchwheel-rally-obstacle-atlas.png";
function cover(ctx,img,w,h,progress=0){if(!img.complete)return;const s=Math.max(w/img.width,h/img.height)*1.22,dw=img.width*s,dh=img.height*s,p=clamp(progress/1000);ctx.drawImage(img,-(dw-w)*p,(h-dh)/2,dw,dh)}
function sprite(ctx,img,col,row,cols,rows,x,y,w,h){if(!img.complete)return;let sw=img.naturalWidth/cols,sh=img.naturalHeight/rows;ctx.drawImage(img,col*sw,row*sh,sw,sh,x,y,w,h)}
const vehicleFrames=[{sx:35,sy:96,sw:570,sh:540,wheels:[.14,.585]},{sx:640,sy:102,sw:570,sh:525,wheels:[.18,.82]}];
function raceVisualState(){if(!run)return null;return{direction:1,playerGround:.80,rivalGround:.49,playerWheelAngle:run.player/24,rivalWheelAngle:run.rival/24,sceneryProgress:clamp(run.player/1000),speed:run.speed||0}}
function drawLane(ctx,w,y,offset,alpha){const spacing=64;ctx.fillStyle=`rgba(13,32,27,${alpha})`;ctx.fillRect(0,y-12,w,24);ctx.strokeStyle=`rgba(255,210,93,${alpha+.18})`;ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(w,y);ctx.stroke();ctx.strokeStyle=`rgba(232,255,244,${alpha+.12})`;ctx.lineWidth=3;for(let x=-(offset%spacing);x<w+spacing;x+=spacing){ctx.beginPath();ctx.moveTo(x,y+7);ctx.lineTo(x+24,y+7);ctx.stroke()}}
function drawSpeedDust(ctx,x,y,seed,color){if(!run||run.firstProfile||run.speed<3)return;ctx.save();ctx.fillStyle=color;ctx.strokeStyle=color;ctx.lineCap="round";for(let i=0;i<5;i++){const phase=(run.elapsed*(34+i*5)+seed*17+i*13)%48,yy=y-5-((i*11+seed*7)%22),r=1+(i%3);ctx.globalAlpha=.22+(i%2)*.12;ctx.beginPath();ctx.ellipse(x-30-phase,yy,r*2.8,r,0,0,Math.PI*2);ctx.fill();ctx.lineWidth=1.5;ctx.beginPath();ctx.moveTo(x-46-phase,yy-7);ctx.lineTo(x-70-phase,yy-7);ctx.stroke()}ctx.restore()}
function drawRace(){if(!run)return;const c=$("#raceCanvas"),ctx=c.getContext("2d"),d=+c.dataset.dpr||1,w=c.width/d,h=c.height/d;ctx.setTransform(d,0,0,d,0,0);ctx.clearRect(0,0,w,h);cover(ctx,bg,w,h,run.player);ctx.fillStyle="#061b1852";ctx.fillRect(0,0,w,h);const visual=raceVisualState(),playerGround=h*visual.playerGround,rivalGround=h*visual.rivalGround,camera=run.player-165,scale=w/720;drawLane(ctx,w,rivalGround,run.rival*3,.28);drawLane(ctx,w,playerGround,run.player*4,.48);const l=levels[run.index];l.terrain.forEach((type,i)=>{let x=(l.positions[i]-camera)*scale;if(x<-150||x>w+150||type==="flat")return;let map={stairs:[0,0],mud:[1,0],tunnel:[2,0],gap:[0,1],ice:[1,1],wind:[2,1]}[type];sprite(ctx,obstacles,map[0],map[1],3,2,x-65,playerGround-100,130,100)});let px=(run.player-camera)*scale,rx=(run.rival-camera)*scale+Math.min(92,w*.24);drawSpeedDust(ctx,px,playerGround,0,"#79ffe0");drawSpeedDust(ctx,rx,rivalGround,1,"#ffd077");drawCart(ctx,px,playerGround,0,run.profile,visual.playerWheelAngle);drawCart(ctx,rx,rivalGround,1,presets.circle,visual.rivalWheelAngle);const finishX=(1000-camera)*scale;ctx.fillStyle="#ffe36b";ctx.fillRect(finishX,playerGround-164,8,164);ctx.fillStyle="#fff";ctx.font="bold 12px sans-serif";ctx.fillText("FINISH",finishX-18,playerGround-170)}
function drawCart(ctx,x,ground,cell,p,angle){const frame=vehicleFrames[cell],cartW=clamp(ctx.canvas.clientWidth*.29,128,178),cartH=cartW*(frame.sh/frame.sw),bob=run&&!run.firstProfile?Math.sin(run.elapsed*11+cell)*1.4:0,wheelY=ground-9,wheelR=clamp(cartW*.13,17,23);if(vehicles.complete&&vehicles.naturalWidth){ctx.save();ctx.translate(x,ground-cartH+bob);ctx.scale(-1,1);ctx.drawImage(vehicles,frame.sx,frame.sy,frame.sw,frame.sh,-cartW/2,0,cartW,cartH);ctx.restore()}ctx.save();ctx.strokeStyle=cell?"#ffc463":"#69ffe0";ctx.shadowColor=ctx.strokeStyle;ctx.shadowBlur=5;ctx.lineWidth=3;frame.wheels.forEach(f=>drawWheelShape(ctx,x+(f-.5)*cartW,wheelY,wheelR,p,angle));ctx.restore()}
function drawWheelShape(ctx,x,y,r,p,angle=0){ctx.save();ctx.translate(x,y);ctx.rotate(angle);ctx.fillStyle="#09231fbb";ctx.strokeStyle=ctx.strokeStyle||"#68ffe0";ctx.beginPath();if(p.points?.length>3){p.points.forEach((q,i)=>{let xx=(q.x-.5)*r*2.05,yy=(q.y-.5)*r*2.05;i?ctx.lineTo(xx,yy):ctx.moveTo(xx,yy)});ctx.closePath()}else if(p.kind==="spike"){for(let i=0;i<16;i++){let a=i*Math.PI/8,rr=i%2?r*.62:r;let xx=Math.cos(a)*rr,yy=Math.sin(a)*rr;i?ctx.lineTo(xx,yy):ctx.moveTo(xx,yy)}ctx.closePath()}else{ctx.ellipse(0,0,r*(p.width/.62),r*(p.height/.56),0,0,Math.PI*2)}ctx.fill();ctx.stroke();ctx.beginPath();for(let i=0;i<4;i++){const a=i*Math.PI/2;ctx.moveTo(0,0);ctx.lineTo(Math.cos(a)*r*.72,Math.sin(a)*r*.72)}ctx.stroke();ctx.beginPath();ctx.arc(0,0,Math.max(3,r*.18),0,Math.PI*2);ctx.fillStyle=ctx.strokeStyle;ctx.fill();ctx.restore()}
function padPoint(e){const c=$("#wheelPad"),r=c.getBoundingClientRect();return{x:(e.clientX-r.left)*c.width/r.width,y:(e.clientY-r.top)*c.height/r.height}}
function drawPad(p,activePoints){const c=$("#wheelPad"),ctx=c.getContext("2d");ctx.clearRect(0,0,c.width,c.height);ctx.fillStyle="#dff8ea";ctx.fillRect(0,0,c.width,c.height);ctx.strokeStyle="#0c6c58";ctx.lineWidth=6;ctx.lineJoin="round";ctx.lineCap="round";let pts=activePoints||(p.points?.map(q=>({x:q.x*c.width,y:q.y*c.height})));if(pts?.length){ctx.beginPath();pts.forEach((q,i)=>i?ctx.lineTo(q.x,q.y):ctx.moveTo(q.x,q.y));if(!activePoints)ctx.closePath();ctx.stroke()}else{ctx.save();ctx.translate(c.width/2,c.height/2);ctx.beginPath();if(p.kind==="spike"){for(let i=0;i<16;i++){let a=i*Math.PI/8,r=i%2?30:50;i?ctx.lineTo(Math.cos(a)*r,Math.sin(a)*r):ctx.moveTo(Math.cos(a)*r,Math.sin(a)*r)}ctx.closePath()}else ctx.ellipse(0,0,48*(p.width/.62),42*(p.height/.56),0,0,Math.PI*2);ctx.stroke();ctx.restore()}}
function bind(){$("#start").onclick=()=>{show("stage");renderDynamic()};$("#stageBack").onclick=()=>show("main");$("#enterRace").onclick=()=>startRace(selected);$("#raceTab").onclick=()=>{$("#forgePanel").hidden=true;$("#racePanel").hidden=false;$("#raceTab").classList.add("active");$("#forgeTab").classList.remove("active")};$("#forgeTab").onclick=()=>{$("#forgePanel").hidden=false;$("#racePanel").hidden=true;$("#forgeTab").classList.add("active");$("#raceTab").classList.remove("active")};$("#lensBuy").onclick=buyLens;$("#stageRail").addEventListener("scroll",()=>{clearTimeout(bind.scrollTimer);bind.scrollTimer=setTimeout(nearestCard,100)},{passive:true});$("#battleBack").onclick=()=>openModal("leaveModal");$("#leaveContinue").onclick=()=>closeModal("leaveModal");$("#leaveStage").onclick=()=>{closeModal("leaveModal",false);show("stage");renderDynamic()};$("#tutorialDone").onclick=()=>{save.tutorial=true;persist();closeModal("tutorialModal")};$("#battleHelp").onclick=()=>openModal("helpModal");$("#helpClose").onclick=()=>closeModal("helpModal");$("#retry").onclick=()=>{closeModal("resultModal",false);startRace(run.index)};$("#resultStage").onclick=()=>{closeModal("resultModal",false);show("stage");renderDynamic()};$("#nextRaceButton").onclick=()=>{let n=Math.min(29,run.index+1);closeModal("resultModal",false);startRace(n)};$$('[data-preset]').forEach(b=>b.onclick=()=>commitProfile({...presets[b.dataset.preset]}));const pad=$("#wheelPad");pad.addEventListener("pointerdown",e=>{if(!run||run.ended)return;drawing=true;drawPoints=[padPoint(e)];pad.setPointerCapture(e.pointerId);$("#feedback").textContent=t.drawing});pad.addEventListener("pointermove",e=>{if(!drawing)return;drawPoints.push(padPoint(e));drawPad(run.profile,drawPoints)});const end=e=>{if(!drawing)return;drawing=false;let p=measure(drawPoints,pad.width,pad.height);if(p)commitProfile(p);else drawPad(run.profile)};pad.addEventListener("pointerup",end);pad.addEventListener("pointercancel",end);window.addEventListener("resize",()=>{if(run){resizeCanvas();drawRace()}},{passive:true})}
function buildGuide(){const box=$("#terrainGuide");box.innerHTML=Object.keys(en.terrainNames).map(k=>`<div><strong>${t.terrainNames[k]}</strong><small>${(t.terrainTips||en.terrainTips)[k]}</small></div>`).join("")}
function init(){initLocales();bind();$("#forgeTab").addEventListener("click",()=>feedbackStage(t.forgeReady));buildGuide();setLocale(locale);setTimeout(()=>{$("#loading").hidden=true;show("main")},450)}
window.__SKETCHWHEEL_RALLY_TEST__={levels,presets,measure,profilePass,startRace,snapshot:()=>run?JSON.parse(JSON.stringify(run)):null,visualState:raceVisualState,step:simulate,setProfile:k=>commitProfile(typeof k==="string"?{...presets[k]}:k),finish,setSave:v=>{save={...defaults(),...v};persist();renderDynamic()},getSave:()=>JSON.parse(JSON.stringify(save)),setLocale,show};
document.addEventListener("DOMContentLoaded",init);
})();
